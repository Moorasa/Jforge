# J-FORGE 작업분해 — P0 부트스트랩 · P1 프로젝트 도메인

> PM 산출물. 소스오브트루스: [기획서](기획서_v0.1.md). 각 태스크는 **수용기준(AC)**과 **담당 에이전트**를 갖는다.
> 상태 표기: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료. 보안 태스크(🔒)는 reviewer 통과가 AC.

---

## P0 — 부트스트랩 (프로젝트 뼈대 초기 셋업, 외부참조 없음)

### P0-1. Maven + Spring Boot 3 WAR 스켈레톤  — coder  ✅ 2026-07-01 완료
- [x] `pom.xml`: Java 17, SB 3.4.2, spring-boot-starter-web, **WAR 패키징**, provided tomcat.
- [x] 패키지 베이스 `com.jworks.forge`, `ForgeApplication` + `ServletInitializer` + `web.HealthController`.
- **AC 검증**: `mvn clean package`로 `target/forge.war`(19.7MB) 생성, contextLoads 테스트 통과,
  **Apache Tomcat/10.1.34**(Jakarta)로 기동, `GET /forge/health` → **200** `{"status":"UP","app":"J-FORGE"}`.
- **환경 메모**: 빌드 JDK = `C:\DEV\Java\openjdk-17.0.2...\jdk-17.0.2`. mvn = m2e 캐시
  `~/.m2/wrapper/dists/apache-maven-3.9.9`. 기본 포트 **18080**(8080은 로컬 IAM 점유).

### P0-2. PostgreSQL + MyBatis 배선  — coder  ✅ 2026-07-01 완료(실동작 검증)
- [x] pom: `mybatis-spring-boot-starter 3.0.4` + `postgresql`(runtime).
- [x] `application.yml` 데이터소스: `.../postgres?currentSchema=jforge`, postgres/1234, Hikari.
- [x] MyBatis: `mapper-locations classpath*:mapper/**/*.xml`, `map-underscore-to-camel-case`.
- [x] 스모크 매퍼 `system.mapper.HealthMapper`(인터페이스+XML) + 진단 `GET /health/db`.
- **AC 검증**: 실 서버 **PG 18.4**(postgres DB/jforge 스키마) 접속 성공, 앱 부팅 후
  `GET /forge/health/db` → **200 `{"db":1}`** (MyBatis 인터페이스+XML 경로로 `SELECT 1`).
- 메모: pg_hba.conf에 클라이언트 `10.20.30.85/32` 허용 필요했음(서버측 조치 완료).

### P0-3. 코어 스키마 DDL  — coder (reviewer 네이밍 검수)  ✅ 2026-07-01 적용 완료
- [x] `src/main/resources/db/ddl/V1__core_schema.sql`: 5개 테이블 + PK/FK/CHECK/인덱스, `jforge` 스키마.
- [x] 대문자 스네이크/_CODE/_ID/_YN, JSONB(DEFINITION/PROP_SCHEMA/FILE_LIST), 기획서 5장 컬럼 반영.
- **AC 검증**: 실 서버에 적용 성공 → jforge 스키마에 5테이블 생성 확인
  (tb_frg_common_code/gen_hist/module_type/project/screen). PG는 미인용 식별자 소문자 저장(정상).

### P0-4. 번들 런타임 반입  — template-author (+ coder 배치)  ✅ 2026-07-01 완료
- [x] `admin/common/header.jsp` 매니페스트 기준 정적자산 복사 → `src/main/resources/static/`:
      jworks 6(css+js), admin/common CSS 7, admin/common JS 12 = **31개** + init.css 참조 폰트 17개.
- [x] **jQuery 3.7.1** 다운로드 반입(code.jquery.com, 87KB, v3.7.1 확인). 3.4.1 미반입.
- [x] **JWORKS 저작권 배너 제거** — 16개 JS 최상단 `/*! ...JWORKS...All rights reserved...*/` 삭제,
      전체 잔존 0건. jQuery 자체 OpenJS 라이선스는 유지(JWORKS 배너 아님).
- [x] `js-singleton.jsp`/`js-constants.jsp`(서버주입)와 header 매니페스트 자체 → **P0-5로 이관**.
- **AC 검증**: 부팅 후 5개 자산 전부 HTTP 200 서빙, `commonSection.js` 첫 줄=설명주석(배너 제거 확인).
- **주의**: P0-5의 header.jsp는 jQuery 참조를 **3.7.1**로 써야 함(원본은 3.4.1).

### P0-5. 도그푸딩 base 레이아웃  — coder  ✅ 2026-07-01 완료
- [x] JSP 뷰 배선: pom `tomcat-embed-jasper`+JSTL(Jakarta), `application.yml` view prefix/suffix.
- [x] shell `admin/home.jsp`(header/menu/breadcrumb 슬롯) + `common/header.jsp`(우리 매니페스트,
      **jQuery 3.7.1**, context-path EL, 배너 없음) + `js-singleton.jsp`/`js-constants.jsp`(서버주입).
- [x] `HomeController` GET `/`,`/admin` → `admin/home`. JS `MagicIAM_JSForgeAdmin` 네임스페이스+IIFE+`__defined`.
- **AC 검증**: `GET /forge/admin` → **200**, 렌더 HTML에 jquery-3.7.1 참조·`/forge/` context-path·
  `MagicIAM_JSForgeAdmin` 존재, 스크립트릿 0·JWORKS 배너 0. JSP는 java -jar WAR+내장Tomcat에서 렌더됨.

---

## P1 — 프로젝트 도메인 (+ 경로안전 계층)

### P1-1. 🔒 경로안전 계층 (PathSafetyService)  — coder → reviewer → tester  ✅ 2026-07-01 완료
- [x] `gen/safety/PathSafetyService.resolveSafeWritePath(root, rel)`: canonical 정규화 후 루트 하위 포함 검증.
- [x] 확장자 화이트리스트(jsp/js/css/java/xml). `..`/절대/드라이브(:)/UNC/루트앵커/심볼릭링크/불법문자 차단.
- **AC 검증**: `PathSafetyServiceTest` **14개 통과**(상위탈출2·절대2·UNC·콜론·비허용확장자2·무확장·불법문자·빈값·정상2). reviewer 자기점검서 InvalidPathException 누수 보강 반영.
- **미검증 1건**: 심링크 탈출 테스트는 이 PC 심링크 생성 권한 부족으로 **skip**(로직·테스트는 존재, 권한 환경에서 재검증 필요).
- → 이후 모든 파일쓰기 태스크(P4 생성엔진)의 **선행 의존 충족**.

### P1-2. TB_FRG_PROJECT CRUD  — coder  ✅ 2026-07-01 완료(실 DB 검증)
- [x] `project.*` 풀스택: domain/dto(record+빈검증)/mapper(IF+XML)/service/web + 공통 예외핸들러.
- [x] REST `/api/projects` — GET 목록/단건, POST 생성, PUT 수정, DELETE 논리삭제(USE_YN='N').
- [x] 컬럼 명시 SELECT(`<sql id="projectColumns">`), `SELECT *` 없음. PG identity 생성키 `keyColumn="project_id"`(소문자).
- **AC 검증**: CREATE 201→GET 200→UPDATE 200→DELETE 204→GET 404, 상대경로/빈이름 입력검증 400.
- 주의: PG useGeneratedKeys keyColumn은 실제 소문자 컬럼명이어야 함(대문자 시 "칼럼 없음" 오류).

### P1-3. 타겟폴더 설정 UI (도그푸딩 3종)  — coder  ✅ 2026-07-01 완료
- [x] 3종 세트: `views/admin/project/list.jsp` + `static/js/admin/project/projectList.js`
      (`MagicIAM_JSForgeAdminProject` IIFE+`__defined`) + `static/css/admin/project/projectList.css`.
- [x] `/api/projects` CRUD 소비(목록/등록/삭제), TARGET_ROOT_PATH 절대경로 클라 사전 힌트.
- **AC 검증**: `GET /admin/projects` 200, 3종 참조·서빙 200, 스크립트릿 0.

### P1-4. TB_FRG_COMMON_CODE 시드  — coder  ✅ 2026-07-01 완료
- [x] `db/ddl/V2__common_code_seed.sql`(멱등 ON CONFLICT): 7그룹 20코드 실서버 적용.
      ARCHETYPE/ROLE/VIEW_TYPE/MODULE_CATEGORY/DB_TYPE/SCREEN_STATUS/GEN_RESULT.
- [x] `code.*`: CommonCode/Mapper(IF+XML)/Controller `GET /api/common-codes/{grpCode}`.
- **AC 검증**: VIEW_TYPE 4·ARCHETYPE 3·MODULE_CATEGORY 5건 정확 반환(SORT_ORDER 순).

---

## 의존 순서
```
P0-1 → P0-2 → P0-3 → P0-4 → P0-5
                 └──────────────→ P1-2 → P1-3
P1-1(경로안전) ─── (모든 파일쓰기 태스크의 선행) ──→ P4 생성엔진
P0-3 → P1-4
```

## 이번 스프린트 착수 후보 (첫 2개)
1. **P0-1** Maven+SB3 WAR 스켈레톤 (coder)
2. **P1-1** 경로안전 계층 (coder→reviewer→tester) — 병렬 착수 가능, 생성엔진의 병목 선행이라 조기 확보 권장.
