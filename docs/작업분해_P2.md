# J-FORGE 작업분해 — P2 카탈로그/스키마 엔진

> **✅ P2 전체 완료 (2026-07-01)** — P2-1~P2-5 전 태스크 완료. MODULE_TYPE 시드(TableView·SearchFilterBar·Toolbar) + 조회 API + 속성스키마 폼렌더(도그푸딩) 확립. 폼렌더는 P3 스튜디오 우측 속성패널로 승격 예정.
>
> PM 산출물. 소스오브트루스: [기획서](기획서_v0.1.md) 2장(모듈 팔레트) · 5장(TB_FRG_MODULE_TYPE) · 9장(scaffold-module-type) · 10장(로드맵).
> 선행: [P0-P1 작업분해](작업분해_P0-P1.md) 전량 완료(✅ 2026-07-01).
> 상태 표기: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료. 보안 태스크(🔒)는 reviewer 통과가 AC.
> **P2 정의**(기획서 10장): MODULE_TYPE 시드(TableView·SearchFilterBar·Toolbar) + 속성스키마 폼 렌더.

---

## P2 범위 요약

P1까지 확립된 사실(재확인, 변경 금지):
- `TB_FRG_MODULE_TYPE`는 **이미 V1 DDL(P0-3)에 생성됨** — 컬럼: `MODULE_TYPE_CODE`(PK), `MODULE_NAME`,
  `CATEGORY_CODE`, `PROP_SCHEMA_JSON`(JSONB), `TEMPLATE_KEY`, `PREVIEW_KEY`, `USE_YN`, `SORT_ORDER`, `REG/MOD_DTM`.
  → **P2에서는 테이블 재생성 없이 시드(V3)만 추가**. 컬럼 부족이 발견되면 V3에서 `ALTER`로 보강.
- `CATEGORY_CODE`의 근거값은 V2 시드의 `MODULE_CATEGORY` 그룹: `VIEW/FILTER/ACTION/DETAIL/WIDGET`.
- 컨벤션(확립됨, 준수 필수):
  - 네이밍: 대문자 스네이크, `_CODE`/`_ID`/`_YN`. **`SELECT *` 금지 → 컬럼 명시 + `<sql id>` include**.
  - MyBatis: 매퍼 인터페이스 + XML, `map-underscore-to-camel-case` 의존, PG identity면 `keyColumn` 소문자.
  - 논리삭제: `USE_YN='N'` (물리삭제 금지). 조회는 `WHERE USE_YN='Y'`.
  - 패키지: `com.jworks.forge.<domain>.{domain,dto,mapper,service,web}`. 예외는 `common.web.ApiExceptionHandler` 재사용.
  - 도그푸딩 3종: `views/admin/<d>/*.jsp` + `static/js/admin/<d>/*.js`(네임스페이스+IIFE+`__defined`) + `static/css/admin/<d>/*.css`. **스크립트릿 0, JWORKS 배너 0**.

P2 산출 도메인 패키지: **`com.jworks.forge.catalog`** (module-type 카탈로그).

---

## P2-1. TB_FRG_MODULE_TYPE 시드 DDL (V3) — coder (reviewer 네이밍/스키마 검수 🔒)  ✅ 2026-07-01 완료(PASS)

> ✅ 2026-07-01 완료 (coder). reviewer 네이밍/스키마 검수(🔒) **PASS**(blocker/high 0건) — 네이밍·멱등성(ON CONFLICT=PK 일치)·PROP_SCHEMA_JSON(문서 §3·라이브 API 완전 일치)·CATEGORY_CODE(MODULE_CATEGORY 존재) 전부 통과. CATEGORY FK 부재는 의도된 설계(low, 액션 불요).

- [x] `src/main/resources/db/ddl/V3__module_type_seed.sql`: 멱등(`ON CONFLICT (MODULE_TYPE_CODE) DO NOTHING`), `SET search_path TO jforge`.
- [x] 최소 3개 시드행: `TABLE_VIEW`(CATEGORY_CODE=`VIEW`), `SEARCH_FILTER_BAR`(=`FILTER`), `TOOLBAR`(=`ACTION`).
- [x] 각 행의 `PROP_SCHEMA_JSON`은 P2-2 스키마 규약(아래)을 따르는 JSONB. `TEMPLATE_KEY`/`PREVIEW_KEY`는
      **P4/P3 산출 예정 키의 예약 문자열**로 채운다(`module/tableView`·`preview/tableView`, `module/searchFilterBar`·`preview/searchFilterBar`, `module/toolbar`·`preview/toolbar`) — 파일 실존은 P2 범위 아님.
- [x] `CATEGORY_CODE` 값은 반드시 V2의 `MODULE_CATEGORY` 그룹에 존재하는 코드여야 함(FK는 없으나 시드 정합성 규칙).
- **AC 검증 결과** (실서버 localhost:5432 / DB postgres / 스키마 jforge, psql 18):
  - ✅ V3 적용: `INSERT 0 3`. 조회 결과 3행, `SORT_ORDER` 오름차순 = TABLE_VIEW(VIEW,1) / SEARCH_FILTER_BAR(FILTER,2) / TOOLBAR(ACTION,3).
    `MODULE_CATEGORY` 시드와 JOIN 검증 시 3행 모두 `category_valid=t`(전부 존재).
  - ✅ 멱등: 동일 파일 재실행 시 `INSERT 0 0`(중복 삽입 0), 종료코드 0(오류 0), 총 행수 3 유지.
  - ✅ 각 `PROP_SCHEMA_JSON` 유효 JSONB이며 `title`·`fields` 키 보유(`? 'title'=t`, `? 'fields'=t`), `fields`는 array(field_cnt: TABLE_VIEW 5 / SEARCH_FILTER_BAR 3 / TOOLBAR 1).
- **메모**: V1 DDL이 `TEMPLATE_KEY`/`PREVIEW_KEY` 등 스키마 문서 필드를 이미 전부 보유 → **ALTER 보강 불필요, 시드 전용**으로 처리함. PROP_SCHEMA_JSON은 스키마_PROP_SCHEMA.md §3 확정 JSON 원문을 그대로 JSONB 캐스팅 투입. **reviewer 검토 대상**(네이밍/스키마 정합).
- **의존**: P0-3(테이블), P1-4(MODULE_CATEGORY 시드). — 둘 다 충족 확인됨.

---

## P2-2. PROP_SCHEMA_JSON 스키마 규약 확정 + 3모듈 초안 — template-author (초안) → reviewer (규약 검수)  ✅ 2026-07-01 완료

기획서 2장 "주요 속성"을 폼 렌더 가능한 선언 스키마로 정규화한다. 이 규약은 P3 프리뷰/P4 생성기와 P2-5 폼렌더가 **공유하는 계약**이므로 먼저 고정한다.

- [x] **스키마 규약 문서화**([docs/스키마_PROP_SCHEMA.md](스키마_PROP_SCHEMA.md)): 최상위 `{ title, fields[] }`.
      `fields[]` 각 원소 필수키: `key`(속성 식별자), `label`(라벨), `type`, `required`(bool), `default`.
      `type` 허용 집합(P2 확정): `text`, `number`, `boolean`(체크박스), `select`(→`options[]`), `columns`(반복 행 그리드),
      `chips`(문자열 배열, 3모듈 미사용·예약). 미지원 type은 폼렌더가 안전하게 무시(스킵)하고 콘솔 경고.
- [x] **TABLE_VIEW 초안**(기획서 2장 근거 commonListTableView.js 실측 검증): `columns`(반복행: `name`,`displayName`,`displayYn`,`sortYn`),
      `selectMode`(select: none/checkbox/radio), `pagingYn`(boolean), `excelYn`/`csvYn`(boolean).
- [x] **SEARCH_FILTER_BAR 초안**(getCurrentSearchData 근거): `filters`(반복행: 필드명·라벨·select옵션), `keywordYn`(boolean),
      `dateRangeYn`(boolean, datepicker).
- [x] **TOOLBAR 초안**: `buttons`(반복행: `actionCode`(add/delete/save 등),`label`,`styleClass`).
- **AC 검증 결과**:
  - ✅ 3개 스키마 모두 최상위 `{title,fields[]}` + 필수키 5종(`key/label/type/required/default`) 100% 보유.
  - ✅ 모든 `type`이 허용 집합 소속, `select`→`options[]`/`columns`→`columns[]` 하위정의 포함(셀 중첩 없음).
  - ✅ 기획서 2장 "주요 속성" 매핑표(§4) 작성, 누락 0건.
  - 🔒 reviewer 검수(PASS, 이슈있음→반영완료): HIGH 지적 "표시 문자열(label 등) XSS 신뢰경계 미명시" → §2.1 신뢰경계 절 신설로 해결(구조 방어는 `key` 화이트리스트만, 나머지는 소비자 이스케이프 책임 명문화). MEDIUM "FreeMarker 예약어 충돌" → `key`는 인터폴레이션 조립 금지·맵 조회만 사용하도록 명문화. LOW(chips 미사용 고지, 버전표기) 반영.
- **메모**: 이 산출이 P2-1 시드의 JSON 본문과 P2-5 폼렌더 입력이 되었다(그대로 소비 확인됨).
- **의존**: 없음(문서 선행). P2-1·P2-5의 선행.

---

## P2-3. 모듈 카탈로그 조회 API (백엔드) — coder

> ✅ 2026-07-01 완료 (coder). reviewer 보안/정합 리뷰는 P2-4에서 수행.

- [x] `catalog.*` 풀스택: `domain.ModuleType`(POJO), `mapper.ModuleTypeMapper`(IF+XML), `service.ModuleTypeService`, `web.ModuleTypeController`.
- [x] REST `GET /api/module-types` (활성 목록, SORT_ORDER 순), `GET /api/module-types/{code}` (단건: PROP_SCHEMA_JSON 포함).
- [x] (옵션) `GET /api/module-types?category={CODE}` 카테고리 필터 — 구현 완료.
- [x] 컬럼 명시 SELECT(`<sql id="moduleTypeColumns">`), `SELECT *` 없음. 조회는 `USE_YN='Y'`.
- [x] `PROP_SCHEMA_JSON`은 JSONB → 서버는 **원문 JSON 트리로 전달**(가공하지 않음). 폼렌더가 클라에서 해석.
- **AC 검증 결과** (재빌드 후 실서버 localhost:18080/forge 구동, curl):
  - ✅ `GET /api/module-types` → **200, 3건**, `SORT_ORDER` 오름차순(TABLE_VIEW 1 / SEARCH_FILTER_BAR 2 / TOOLBAR 3). 각 원소에 `moduleTypeCode`/`moduleName`/`categoryCode`/`propSchemaJson`/`templateKey`/`previewKey`/`useYn`/`sortOrder` 존재.
  - ✅ `GET /api/module-types/TABLE_VIEW` → **200**. `propSchemaJson`이 **문자열이 아닌 JSON 객체**로 응답에 실림(재직렬화 왕복 없이 `title`·`fields[]` 원문 그대로) — §2.1 신뢰경계(재해석/가공 금지) 준수.
  - ✅ `GET /api/module-types/NOPE` → **404** (`{"status":404,"error":"Not Found","message":"module type not found: NOPE"}`, `ApiExceptionHandler` 경유, `NotFoundException` 재사용).
  - ✅ `GET /api/module-types?category=VIEW` → **200, 1건**(TABLE_VIEW).
  - ✅ 매퍼 XML `SELECT *` **0건**(grep: 주석 문자열만 매칭, 실 쿼리 0).
- **메모(구현 결정)**: JSONB→Java 매핑은 **매퍼에서 `PROP_SCHEMA_JSON::text AS PROP_SCHEMA_JSON`로 원문 문자열 추출** →
  도메인 게터에 `@JsonRawValue` 부착으로 응답 직렬화 시 **날 것(raw) JSON 트리로 삽입**(String 이스케이프 왕복 없음 → 내용 훼손·인젝션 여지 최소). 기존 project 도메인은 JSONB 컬럼이 없어(TB_FRG_PROJECT) 참조 패턴 부재 → 문서 §2.1 계약에 맞춰 위 방식 신설. 빈값 방어로 게터가 공백 시 `{}` 반환(항상 유효 JSON 토큰). PathVariable `{code}`는 MyBatis `#{}` 바인딩만 사용(`${}` 0). **reviewer 검토 대상**(P2-4).
- **의존**: P2-1(시드 데이터 존재해야 조회 검증 가능) — 충족 확인됨.

---

## P2-4. 🔒 카탈로그 API 보안/정합 리뷰 — reviewer  ✅ 2026-07-01 완료(PASS)

- [x] `{code}` PathVariable에 대한 입력 처리 검토: SQL 인젝션 방어(MyBatis `#{}` 파라미터 바인딩 확인, `${}` 사용 0).
- [x] `PROP_SCHEMA_JSON` 응답 경로 검토: 서버가 신뢰경계에서 JSON을 **재직렬화하며 스크립트 주입 여지가 없는지**
      (문자열 그대로 전달 시 클라 렌더 단계의 XSS 책임 → P2-5 AC로 연결).
- [x] 응답 크기/오류 포맷이 `ApiExceptionHandler` 규약과 일치하는지.
- **AC 검증**: `#{}` 바인딩만 사용(`${}` 0건, grep 확인). 라이브 인젝션 시도
  `.../TABLE_VIEW'; DROP TABLE--` → 404, `?category=VIEW' OR '1'='1` → 200 빈배열(SQL 에러 없음).
  `PROP_SCHEMA_JSON::text AS ...` + `@JsonRawValue` 조합은 컬럼이 `JSONB NOT NULL DEFAULT '{}'::jsonb`(V1 DDL)라
  PostgreSQL이 저장 시점에 유효 JSON을 강제 → 응답에 잘못된 JSON이 실릴 경로가 구조적으로 없음(이중 이스케이프 없음).
  오류 포맷은 `NotFoundException`→`ApiExceptionHandler` 규약과 일치(`{status,error,message}`).
- **메모**: 임의 파일쓰기/경로탈출은 P2에 없음(조회·시드 위주). 보안 초점은 **인젝션(SQL/템플릿) + 폼렌더 XSS 경계**.
  **후속 인계**: `label`/`title` 등 표시 문자열은 서버가 이스케이프하지 않고 원문 전달(계약대로 정상) →
  **P2-5는 반드시 `innerHTML` 금지, `textContent`/`.text()`만 사용**해야 XSS 방어됨(P2-5 AC 필수 항목).
- **의존**: P2-3.

---

## P2-5. 속성스키마 폼 렌더 UI (도그푸딩 3종) — coder (reviewer XSS 확인 🔒)  ✅ 2026-07-01 완료

> ✅ 2026-07-01 완료 (coder). reviewer XSS 확인(🔒) **PASS** — `schemaFormRenderer.js`/`catalogList.js`/`list.jsp` 전수 검토, 자유문자열 삽입 지점(title/label/option.label/column.label/chip값) 전부 `textContent`/`createElement`+`appendChild`만 사용, `innerHTML`/`.html()` 실제 DOM삽입 0건(매칭 3건은 주석). `<img onerror>`/`<script>` 라벨도 텍스트로만 이스케이프 표시되어 실행 불가로 코드리뷰 결론.

PROP_SCHEMA_JSON을 입력받아 **동적 속성 폼**을 그리는 도그푸딩 화면. P3 스튜디오 우측 속성패널의 전신(前身)이므로 재사용 가능하게 모듈화.

- [x] 3종 세트:
  - `views/admin/catalog/list.jsp` (좌: 모듈 카탈로그 목록, 우: 선택 모듈의 속성폼 프리뷰 슬롯). 스크립트릿 0, JWORKS 배너 0.
  - `static/js/admin/catalog/catalogList.js` (`MagicIAM_JSForgeAdminCatalog` 네임스페이스+IIFE+`__defined`). API 소비·화면 결합 담당.
  - `static/css/admin/catalog/catalogList.css` (좌/우 2단 grid, `required` 별표 `.frg-required` 등).
- [x] `SchemaFormRenderer` 렌더 함수 — **별도 순수모듈** `static/js/admin/catalog/schemaFormRenderer.js`
      (`window.MagicIAM_JSForgeSchemaRenderer.render(schema)` → `DocumentFragment`). 화면(list.jsp)과 미결합.
      입력=PROP_SCHEMA_JSON 객체 → 출력=DOM. 지원 type: `text/number/boolean/select/columns/chips`. 미지원 type 스킵+`console.warn`(에러 미발생, forward-compat).
- [x] `/api/module-types` 소비(목록), `/api/module-types/{code}` 소비(선택 시 스키마 로드 후 폼 렌더).
- [x] 페이지 진입 컨트롤러 `catalog.web.CatalogPageController` `GET /admin/catalog` → `admin/catalog/list`.
- **AC 검증 결과** (재빌드 후 실서버 localhost:18080/forge 구동, curl):
  - ✅ `GET /forge/admin/catalog` → **200**. 3종 자산 전부 200: `js/admin/catalog/catalogList.js`, `js/admin/catalog/schemaFormRenderer.js`, `css/admin/catalog/catalogList.css`. API `GET /api/module-types`·`/api/module-types/TABLE_VIEW` → 200.
  - ✅ 렌더된 HTML **스크립트릿 0건**(`grep -c '<%'` = 0). **JWORKS 저작권 배너 0건** — HTML의 "JWORKS" 12건은 전부 `external/jworks/*`(스낵바·툴팁·스피너 등 번들 라이브러리) 로드 경로이며, 저작권/copyright 문자열 0(P1-3 project 화면과 동일 header.jsp 매니페스트).
  - ✅ 카탈로그 목록 API가 **3모듈** 반환: `['TABLE_VIEW','SEARCH_FILTER_BAR','TOOLBAR']`(SORT_ORDER 순). 좌측 목록은 `moduleName`+`categoryCode` 뱃지+`moduleTypeCode`를 전부 `textContent`로만 렌더.
  - ✅ TABLE_VIEW 스키마 필드 = `columns`(type=columns, 반복행 그리드)·`selectMode`(type=select)·`pagingYn/excelYn/csvYn`(type=boolean 체크박스) — **스키마대로 렌더 경로 확인**(각 type별 렌더러 dispatch). SEARCH_FILTER_BAR(filters/keywordYn/dateRangeYn)·TOOLBAR(buttons) 스키마도 200 정상.
  - 🔒 **XSS**: `grep -rn "innerHTML" .../js/admin/catalog/` → DOM삽입 **0건**(매칭 3건은 전부 주석 내 "innerHTML 금지" 설명 문자열). jQuery `.html()` DOM삽입도 **0건**. 모든 표시 문자열(`title`/`label`/`options[].label`/`columns[].label`/chip 값)은 `document.createTextNode`/`.textContent`로만 삽입 → 서버가 이스케이프하지 않는 신뢰경계(§2.1) 방어. `key`/`code`류 식별자는 `setAttribute`. **P2-4 인계 필수 AC(innerHTML 금지) 충족.** reviewer 악의 `label`(`<img onerror>`) 시드 미실행 확인 대기.
  - ✅ `required` 필드(TABLE_VIEW: columns/selectMode, TOOLBAR: buttons) 시각 구분 — 라벨 뒤 `<span class="frg-required">*</span>`(빨간 별표, `aria-label="필수"`) 부착.
- **메모**: 폼렌더 DOM 생성 로직을 화면(catalogList.js)과 **완전 분리**해 순수 함수(`schemaFormRenderer.render`)로 구현 — P3 3-pane 우측 패널로 그대로 승격 가능. 렌더러는 스키마가 `null`/비객체여도 안전 메시지 반환(방어). `propSchemaJson`은 서버 `@JsonRawValue`로 이미 JSON 객체이나, 문자열로 올 경우 `JSON.parse` 방어 경로 포함. **reviewer XSS 검토 대상**(🔒).
- **의존**: P2-2(스키마 규약), P2-3(조회 API), P2-4(보안 리뷰 통과) — 전부 충족.

---

## 의존 순서

```
P2-2(스키마 규약·초안) ──┬─→ P2-1(V3 시드 DDL) ──→ P2-3(조회 API) ──→ P2-4(🔒보안리뷰) ──→ P2-5(폼렌더 3종, 🔒XSS)
                         └───────────────────────────────────────────────────────────────→ P2-5
[선행 P1: P0-3 테이블 / P1-4 MODULE_CATEGORY 시드 / ApiExceptionHandler·NotFoundException 재사용]
```

- P2-2는 문서 선행이라 최우선. 확정 전 P2-1/P2-5 착수 시 재작업 위험.
- P2-3은 P2-1 시드 데이터가 있어야 실검증 가능(단, 코드 골격은 시드와 병렬 가능).

## 이번 스프린트 착수 후보 (첫 2개)

1. **P2-2** PROP_SCHEMA_JSON 스키마 규약 확정 + 3모듈 초안 (template-author → reviewer) — 나머지 전부의 계약이라 최우선.
2. **P2-1** V3 모듈타입 시드 DDL (coder → reviewer 🔒) — 규약 초안이 서면 즉시 착수, P2-3 검증의 선행.
