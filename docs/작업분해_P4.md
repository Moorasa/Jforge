# J-FORGE 작업분해 — P4 생성 엔진

> **⏳ P4 착수 대기 (2026-07-01)** — P0~P3 전 페이즈 완료(부트스트랩·프로젝트도메인+경로안전·카탈로그/스키마·3-pane 스튜디오+라이브프리뷰). 이제 로드맵 **P4 생성 엔진**: `FreeMarker → 3종+stub 폴더 직접쓰기 + GEN_HIST`(기획서 10장). **P4는 빌더의 존재 이유**(DEFINITION_JSON을 실제 타겟 폴더의 JSP+JS+CSS 3종으로 산출) 이자 **보안 핵심**(임의 파일쓰기·경로탈출·템플릿 인젝션) 이다.
>
> PM 산출물. 소스오브트루스: [기획서](기획서_v0.1.md) **0장 4대 설계축(특히 3번 번들 런타임 복사·참조, 4번 타겟 폴더 직접 파일쓰기)** · 4장(화면 조립 계약: 1화면 = N개 3종세트 + iframe include 배선) · 5장(TB_FRG_GEN_HIST) · 6장(생성 엔진 파이프라인 · 공통코드 2층위 · 경로안전 필수) · 10장(**P4 정의**).
> 입력 계약: [스키마_DEFINITION_JSON.md](스키마_DEFINITION_JSON.md)(생성기 입력, **§5 신뢰경계 = 소비자 이스케이프 책임**, §1.1 stem 규칙, PathSafetyService 이중경계) · [스키마_PROP_SCHEMA.md](스키마_PROP_SCHEMA.md)(모듈 props 필드 정의).
> 선행: [P0-P1](작업분해_P0-P1.md)(✅) · [P2](작업분해_P2.md)(✅) · [P3](작업분해_P3.md)(✅).
> 상태 표기: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료. **본 문서 작성 시점 전 항목 `[ ]` 대기.** 보안 태스크(🔒)는 reviewer 통과가 AC.

---

## P4 범위 요약 / 경계선 (매우 중요)

**P4가 하는 것**: DB에 저장된 `DEFINITION_JSON`(P3 산출)을 입력으로 받아 → `TemplateContext` 구성 → **FreeMarker 렌더**(코드생성 전용 엔진, 빌더 자신의 JSP 뷰와 분리) → `PathSafetyService` 경로해석 → **원자적 파일쓰기**(타겟 폴더 직접) → `TB_FRG_GEN_HIST` 기록. 스튜디오에 **"생성" 버튼**을 추가(P3 "저장"과 구분: 저장=DB, 생성=파일). tester **골든파일 스냅샷** 검증.

**P4의 최소 완주 목표(MVP, 이 페이즈 완료 판정 기준)**:
> **최소 1개 아키타입(`MGMT_LIST_DETAIL`)의 1개 뷰(`TABLE_VIEW`가 배치된 `listArea`) 기준으로 shell+List+ListTableView 3종(JSP/JS/CSS)을 실제 타겟 폴더(`TARGET_ROOT_PATH` 하위)에 경로안전하게 쓰고, GEN_HIST에 성공 이력을 남기며, tester 골든테스트를 통과한다.**

**P4가 하지 않는 것(P5/P6로 분리)**:
- 다른 아키타입(`SIMPLE_LIST` 전용 배선의 완결, `DUAL_LAYOUT` 좌우 2단) 완전 생성 — **P5**. (단, `SIMPLE_LIST`는 MGMT의 상세슬롯을 뺀 부분집합이라 P4-6에서 저위험 확장 후보로만 다룸.)
- 다른 모듈(Tree/Card/Form/Detail/AssociateTabs/Popup) 템플릿 — **P5**.
- Controller/Mapper 실 구현 로직 — P4는 **빈 stub 폴더/스텁 파일**까지만(기획서 10장 "3종+stub 폴더").
- 공통코드 심화(런타임 버전관리·라운드트립·공통추출 정교화) — **P6**. P4는 **최소 정책**만(P4-6).
- 덮어쓰기 diff UI·백업 히스토리 브라우저 — P4는 **백업(.bak)·거부 정책**의 서버측 최소 구현만.

## P3/P2/P1에서 확립된 선행 자산 (재확인, 코드로 검증됨 — 변경 금지)

- **🔒 PathSafetyService** (`src/main/java/com/jworks/forge/gen/safety/PathSafetyService.java`, P1-1) — **P4 파일쓰기의 필수 최종 방어선**. 실 시그니처:
  - `Path resolveSafeWritePath(Path targetRoot, String relativePath)` — 반환: 정규화된·루트하위 보장 절대경로. 위반 시 `PathSafetyException`.
  - 차단: 콜론(`:`)/드라이브/ADS, UNC/루트앵커, 명시적 `..`, 정규화 후 루트 탈출, 심볼릭링크 루트탈출, **확장자 화이트리스트**.
  - `DEFAULT_ALLOWED_EXTENSIONS = {jsp, js, css, java, xml}` (public static). 생성자 `PathSafetyService(Set<String>)`로 화이트리스트 주입 가능.
  - **모든 쓰기 경로(3종+stub 포함)는 예외 없이 이 메서드를 통과**해야 한다. relativePath는 항상 **정적 세그먼트 + 화이트리스트 통과한 stem**으로만 조립(자유문자열 파일명 금지).
- **TB_FRG_PROJECT.TARGET_ROOT_PATH** (P1-2) — 생성물이 쓰일 루트. `PACKAGE_BASE`/`JSP_BASE_PATH`/`JS_BASE_PATH`/`CSS_BASE_PATH`/`RUNTIME_VER`도 이 테이블에 존재 → TemplateContext 메타 소스.
- **TB_FRG_SCREEN.DEFINITION_JSON** + `GET /api/screens/{id}`(P3-1, DEFINITION_JSON 원문 트리 포함) — 생성 입력. 화면 메타(`stem`/`archetypeCode`/`roleCode`/`projectId`)도 여기서.
- **TB_FRG_GEN_HIST** (V1 DDL에 **이미 존재**, 재생성 금지) — 컬럼: `GEN_HIST_ID`(PK IDENTITY), `SCREEN_ID`(FK→SCREEN), `GEN_AT`(DEFAULT now()), `FILE_LIST_JSON`(JSONB DEFAULT '[]'), `RESULT_CODE`(NOT NULL: `SUCCESS`/`PARTIAL`/`FAIL`). `IX_TB_FRG_GEN_HIST_SCREEN` 존재. → **P4에서 테이블 재생성 없음**, 컬럼 부족시에만 V_ ALTER.
- **TB_FRG_MODULE_TYPE.TEMPLATE_KEY** (P2-1 시드, V3) — 확정값: `TABLE_VIEW`→`module/tableView`, `SEARCH_FILTER_BAR`→`module/searchFilterBar`, `TOOLBAR`→`module/toolbar`. **P4 FreeMarker 템플릿 파일명 매핑의 소스오브트루스**(문자열 조립 금지, 맵 조회로만 → §5.1). `PREVIEW_KEY`는 프리뷰용(P3)이라 P4 무관.
- **DEFINITION_JSON 계약 §5 신뢰경계**(P3-2, 확정) — **props 자유문자열(label/displayName/styleClass/컬럼값)은 서버가 이스케이프하지 않는다. 소비자=P4가 대상 문법(HTML/JS문자열/CSS)에 맞게 이스케이프**. `stem` 정규식 통과는 경로안전을 대체하지 않음(**이중경계**: 저장시 1차 + P4 PathSafetyService 2차).
- **번들 런타임 매니페스트**(`src/main/webapp/WEB-INF/views/admin/common/header.jsp`, P0-4/5) — 생성물이 참조해야 할 공통 런타임(jQuery 3.7.1 + jworks 6종 + commonList* + js-singleton/js-constants). P4 공통코드 처리(6장)의 대상.
- **컨벤션(P0~P3 확립, 준수 필수)**: 패키지 `com.jworks.forge.<domain>.{domain,dto,mapper,service,web}`. MyBatis `#{}` 바인딩만(`${}` 금지), `SELECT *` 금지+`<sql id>` include, JSONB는 `::text AS`/`::jsbon` 캐스팅+`@JsonRawValue`. 도그푸딩 UI는 네임스페이스+IIFE+`__defined`, 스크립트릿 0, **JWORKS 저작권 배너 0**(생성물도 배너 미포함 — 기획서 11장). 예외는 `common.web.ApiExceptionHandler`/`NotFoundException` 재사용.

P4 산출 도메인 패키지: **`com.jworks.forge.gen`** (이미 `gen.safety` 존재 → `gen.template`/`gen.context`/`gen.pipeline`/`gen.hist`/`gen.web` 하위로 확장). 도그푸딩 UI는 P3 스튜디오(`JWorks_JSForgeAdminStudio`)에 "생성" 버튼 추가.

---

## P4-0. 🔒 생성 파이프라인 계약 문서 확정 — pm/template-author (초안) → reviewer (신뢰경계·경로안전 검수) ✅ 완료(2026-07-02, reviewer PASS blocker/high 0)

P4의 전 태스크가 공유하는 **단일 계약**을 다른 P4 태스크보다 **먼저 고정**한다(P3-2 선례: 계약 선행으로 재작업 방지). 문서 `docs/생성엔진_계약.md` 신설. FreeMarker 템플릿 세트(P4-3), 파이프라인(P4-4), 골든테스트(P4-7)가 전부 이 문서를 참조한다.

> **완료 메모**: `docs/생성엔진_계약.md` 작성 → reviewer 보안검수 PASS(코드 인용 정확성·이스케이프 3문맥·경로안전·맵조회·원자성 확인). 비차단 권고 3건 계약에 반영: §2.3 FreeMarker 위험 빌트인 차단(`ALLOWS_NOTHING_RESOLVER`, `?eval`/`?new` 금지 → P4-1 필수), §3.2/§3.3 unquoted 속성 금지+`"` 항상 엔티티화+인라인 `<script>` `\x3C`, §5.1 심볼릭링크 NOFOLLOW_LINKS 거부. 기본 덮어쓰기 정책=`.bak` 타임스탬프 백업 후 덮어쓰기.

- [x] **아티팩트 매핑 계약**: `MGMT_LIST_DETAIL` + `TABLE_VIEW(listArea)` 1뷰 기준, DEFINITION_JSON → 산출 파일 목록/상대경로 규칙 표. shell `{stem}.jsp` / `{stem}List.jsp`+`.js`+`.css` / `{stem}ListTableView.jsp`+`.js`+`.css` (기획서 4장). 각 파일의 **타겟 상대경로**를 `role`(admin/user) + `JSP_BASE_PATH`/`JS_BASE_PATH`/`CSS_BASE_PATH`로 결정하는 규칙 명문화. stub(Controller/Mapper)은 P4-6에서.
- [x] **TemplateContext 계약**: 템플릿에 노출되는 모델 키 목록 확정 — `stem`/`role`/`archetype`/`packageBase`/각 basePath/`slots`(슬롯별 인스턴스+props). 슬롯→인스턴스→props 접근 경로를 §5.1대로 **맵 조회**로만(문자열 조립 금지) 하도록 규정. (계약 §2)
- [x] 🔒 **이스케이프 정책 확정(핵심)**: DEFINITION_JSON §5 재사용. props 자유문자열이 산출물의 어느 문맥에 들어가는지 분류 → **문맥별 이스케이프 함수** 지정: `htmlText`/`htmlAttr`(HTML), `jsString`(JS 문자열 리터럴), `cssToken`(CSS 화이트리스트). `styleClass`류는 토큰 화이트리스트(공백·`<`·`>`·`"` 배제). **어떤 자유문자열도 이스케이프 없이 원문 삽입 금지** 원칙 표. (계약 §3)
- [x] 🔒 **경로/파일명 안전 계약**: 파일명은 `{정적접미사}` + `stem`(§1.1 정규식 통과값)으로만 조립. 모든 상대경로는 PathSafetyService 통과 필수. 확장자는 `{jsp,js,css,java,xml}` 화이트리스트 내. relativePath에 props값·instanceId 등 **자유문자열 절대 미포함** 명문화. (계약 §4)
- [x] **덮어쓰기/원자성/이력 정책**: 원자적 쓰기(임시파일→ATOMIC_MOVE rename), 기본정책=`.bak` 타임스탬프 백업 후 덮어쓰기, 심볼릭링크 NOFOLLOW_LINKS 거부, 부분실패 시 `RESULT_CODE`(SUCCESS/PARTIAL/FAIL) 판정 기준. (계약 §5)
- **AC**:
  - 아티팩트 매핑 표 + TemplateContext 키 목록 + 🔒이스케이프 정책 표 + 🔒경로안전 계약 + 덮어쓰기/이력 정책 완성. 예시 1건(§6 DEFINITION_JSON 예시 → 산출 파일 목록/경로).
  - 🔒 reviewer 검수 PASS(blocker/high 0): props 자유문자열 → 산출 문맥별 이스케이프 누락 없음(HTML/JS/CSS 3문맥), 경로/파일명 조립에 자유문자열 미포함, PathSafetyService 필수경유 명문화, TEMPLATE_KEY 맵조회(문자열조립 아님) 확인. DEFINITION_JSON §5와 일관성.
- **의존**: 없음(문서 선행). **P4-2/P4-3/P4-4/P4-7 전부의 계약** → 최우선.

---

## P4-1. FreeMarker 엔진 배선 (코드생성 전용 Configuration) — coder ✅ 완료(2026-07-02)

> **완료 메모**: `pom.xml` FreeMarker 2.3.34(순수 lib, spring-boot-starter-freemarker 미도입 → 뷰리졸버 충돌 0). `gen.template.CodeGenTemplateConfig`(전용 Configuration 빈, `templates/gen/` 로더 분리) + `TemplateRenderer`(`templateKey+".ftl"`만 접미, 조립 0) + `TemplateRenderException`. 🔒계약 §2.3 위험 빌트인 차단 반영: `ALLOWS_NOTHING_RESOLVER`(`?new`)·`setAPIBuiltinEnabled(false)`(`?api`)·RETHROW_HANDLER·`number_format=computer`. 스모크 `templates/gen/smoke/hello.ftl` + JUnit 3케이스 PASS, `mvn -DskipTests package` EXIT=0. **엔진 보안설정 검토는 P4-2 reviewer 게이트에 통합**(같은 config에 escaper 등록).

기획서 1장 "생성 템플릿 엔진 FreeMarker(코드생성 전용, 빌더 자신의 뷰 렌더링과 분리)". **pom.xml에 FreeMarker 의존성이 아직 없음**(grep 0건 확인) → 추가가 P4 첫 코드 태스크.

- [ ] `pom.xml`에 FreeMarker 의존성 추가(SB3 호환 버전). **빌더 자신의 뷰는 JSP**이므로 Spring MVC 뷰리졸버로 FreeMarker를 등록하지 **않는다**(뷰 리졸버 충돌 금지) — 순수 라이브러리로만 사용.
- [ ] `com.jworks.forge.gen.template.CodeGenTemplateConfig`(@Configuration) — **코드생성 전용** `freemarker.template.Configuration` 빈. 빌더 뷰 렌더링과 완전 분리(별도 인스턴스, 별도 로딩 경로). 설정: 템플릿 로딩 경로(클래스패스 `templates/gen/` 하위), UTF-8, `TemplateExceptionHandler.RETHROW_HANDLER`(생성 실패를 조용히 삼키지 않음), **number_format `computer`**(천단위 콤마 오염 방지), 자동이스케이프는 태스크 P4-2/P4-0 정책에 맞춰 설정(HTML 출력포맷 등).
- [ ] `gen.template.TemplateRenderer`(@Component) — `String render(String templateKey, Map<String,Object> model)`. `templateKey`는 **TEMPLATE_KEY(맵 조회로 얻은 값) + 아티팩트 접미사** 규칙으로 실제 `.ftl` 파일 해석(문자열 조립·평가 없음, 화이트리스트 경유). 템플릿 미존재/렌더오류는 명확한 예외로.
- **AC**:
  - `pom.xml`에 FreeMarker 의존성 존재, 빌드 성공. `mvn`/래퍼로 앱 기동 시 기존 JSP 뷰 정상(뷰리졸버 회귀 0 — `/admin/studio` 200 유지).
  - `CodeGenTemplateConfig`가 빌더 뷰와 **분리된** Configuration 빈임을 코드로 확인(별도 로더 경로 `templates/gen/`).
  - `TemplateRenderer.render`가 간단한 스모크 `.ftl`(예: `${stem}`) 렌더 성공. 예외 핸들러 RETHROW로 오류 전파 확인.
- **의존**: P4-0(templateKey→파일 규칙). P4-3(실 템플릿)이 이 렌더러를 사용.

---

## P4-2. TemplateContext 구성 + 🔒 문맥별 이스케이프 유틸 — coder (reviewer 🔒 이스케이프 검수) ✅ 완료(2026-07-02, reviewer PASS blocker/high 0)

> **완료 메모**: `gen.context.GenEscaper`(4함수 `htmlText`/`htmlAttr`/`jsString`/`cssToken`, switch/char 단일패스라 치환순서 버그 구조적 배제, `jsString`은 `<`→`\x3C`+`/`→`\/`로 `</script>` 조기종료 차단·U+2028/2029 처리, cssToken 화이트리스트+로그인젝션 방어) + `TemplateContextBuilder`(readTree 데이터 파싱만·props 원문보존·stem `^[a-z][a-zA-Z0-9]*$` 재검증 하드실패·forward-compat 스킵) + `TemplateContextException`. `CodeGenTemplateConfig`에 escaper를 `TemplateMethodModelEx`로 shared variable 등록(P4-1 위험 빌트인 차단 미훼손). 테스트 23/23 PASS. reviewer low 2건(EscapeMethod `getAsString()` 선호, cssToken 혼합순서 테스트)은 선택. **🔒 인계: P4-3 템플릿이 모든 자유문자열을 escaper에 실제로 태우는지가 인젝션 최종 차단점 → P4-7 골든으로 고정.**

DEFINITION_JSON + 프로젝트 메타를 FreeMarker 모델(`Map`)로 변환하고, props 자유문자열을 **산출 문맥별로 안전하게** 만드는 계층. **P4 보안의 첫 관문**.

- [ ] `gen.context.TemplateContextBuilder`(@Component) — 입력: `ForgeScreen`(DEFINITION_JSON 원문) + `ForgeProject`(target/package/basePath/runtimeVer). 출력: P4-0 계약의 TemplateContext 키를 채운 `Map<String,Object>`. DEFINITION_JSON은 Jackson `readTree`로 **데이터로만** 파싱(문자열조립·코드평가 없음, P3-5b 선례). 슬롯/인스턴스/props를 맵 구조로 노출.
- [ ] 🔒 `gen.context.GenEscaper`(또는 FreeMarker `TemplateMethodModel`/커스텀 디렉티브) — 문맥별 이스케이프: `htmlText`/`htmlAttr`(HTML), `jsString`(JS 문자열 리터럴: `'`,`"`,`\`,`</`,`\n`,`U+2028/2029` 등), `cssToken`(CSS 클래스/식별자 화이트리스트). **템플릿에서 자유문자열은 반드시 이 함수를 거쳐 출력**(P4-0 정책). styleClass류는 토큰 화이트리스트(공백·`<`·`>`·`"` 배제, 위반 토큰 드롭 또는 거부).
- [ ] **stem 재확인**: 화면 메타의 `stem`(P3-5b 리뷰 인계 low: stem을 소스오브트루스로)을 §1.1 정규식으로 재검증 후 컨텍스트/파일명에 사용. DEFINITION_JSON 내부 `stem`과 불일치 시 정책(메타 우선 + 경고 또는 하드실패) 확정.
- [ ] **forward-compat**: 미지원 slotKey/moduleTypeCode 인스턴스는 §4대로 스킵+로그(문서 전체 실패 금지). 미지원 props 키 무시.
- **AC**:
  - `TemplateContextBuilder`가 §6 예시 DEFINITION_JSON을 P4-0 키 규격 Map으로 변환(단위테스트: slots/props 접근 경로 검증).
  - 🔒 이스케이프 단위테스트: props에 `<script>`/`"`/`'`/`</script>`/`${x}`/`a b`(공백 classname) 주입 시 각 문맥 출력이 **안전 이스케이프**됨(HTML `&lt;`, JS-string escape, CSS 토큰 드롭/거부). 원문 그대로 새어나가는 케이스 0.
  - stem 정규식 위반값 하드실패(경로/파일명 오염 차단), 메타↔DEFINITION stem 불일치 정책 동작.
  - 🔒 reviewer 검수 PASS(blocker/high 0): 3문맥 이스케이프 완전성, 문자열조립·코드평가 0(readTree 데이터 파싱만), stem 화이트리스트 재검증 확인.
- **의존**: P4-0(계약), P4-1(Configuration에 escaper 등록). P4-3 템플릿이 이 컨텍스트/이스케이퍼를 소비.

---

## P4-3. FreeMarker 템플릿 세트 (MGMT_LIST_DETAIL / TABLE_VIEW 3종) — template-author (reviewer 🔒 인젝션 검수) ✅ 완료(2026-07-02, reviewer FAIL→수정→PASS)

> **완료 메모**: 7개 .ftl(shell/list/listJs/listCss/tableView/tableViewJs/tableViewCss) 작성. props 자유문자열은 문맥별 escaper 경유(displayName→htmlText/jsString, name→htmlAttr/jsString, styleClass→cssToken, actionCode→htmlAttr, filters→htmlAttr/htmlText). commonList/commonListTableView API 배선(`JWorks_JSCommonList.init`/`...TableView.init`), 조건부 슬롯 `<#if>`, 스크립트릿0·배너0·CDN0. **reviewer가 BLOCKER B1 발견(role이 미검증 자유문자열 → shell.ftl 속성·listJs.ftl JS에 원문삽입 → 저장형 XSS/JS인젝션) → 3계층 수정으로 해소**: (1)`TemplateContextBuilder`가 role/archetype도 stem처럼 화이트리스트 재검증 하드실패(`ROLE_REGEX`/`ARCHETYPE_REGEX`, model.put 전), (2)저장경계 `ForgeScreenRequest.roleCode @Pattern`+`ForgeScreenService`의 `isCodeInGroup("ROLE"/"ARCHETYPE")` 검증, (3)shell.ftl `htmlAttr(role)`·listJs.ftl `jsString(NS)`. 골든에 role 인젝션 6종 차단 테스트 추가. 전체 45 테스트 PASS. (재검수는 세션한도로 중단됐으나 메인이 직접 라인검증으로 B1 해소 확인.)

P4 MVP의 핵심 산출: `MGMT_LIST_DETAIL` 아키타입 + `listArea`의 `TABLE_VIEW` 1뷰 기준 **최소 3종 세트 템플릿**. **스코프를 좁게**: 검색/툴바/상세슬롯은 있으면 렌더(옵션), 없으면 생략. MVP는 shell+List+ListTableView 골격 배선 우선.

- [ ] 템플릿 배치: `src/main/resources/templates/gen/` 하위, TEMPLATE_KEY 규칙과 정합. 최소 세트:
  - **shell** `archetype/mgmtListDetail/shell.ftl` → `{stem}.jsp`(layout include + `common/header.jsp` 매니페스트 참조 + List iframe/컨테이너 배선). 기획서 4장 "shell → List → List{View}".
  - **List** `archetype/mgmtListDetail/list.ftl`(+`listJs.ftl`+`listCss.ftl`) → `{stem}List.jsp`/`.js`/`.css`(뷰 전환 컨트롤러 = commonList 참조).
  - **ListTableView** `module/tableView.ftl`(+js+css) → `{stem}ListTableView.jsp`/`.js`/`.css`(TABLE_VIEW props: columns/selectMode/pagingYn/excelYn/csvYn 반영). TEMPLATE_KEY `module/tableView`와 매핑.
- [ ] 🔒 **템플릿 내 자유문자열 전량 이스케이프**(P4-2 GenEscaper 경유). displayName/컬럼값 등 원문 삽입 0. `${}` 직접 출력 시 자동이스케이프 또는 명시 이스케이프. **JWORKS 저작권 배너 미포함**(기획서 11장) — 템플릿에 배너 문자열 0.
- [ ] 번들 런타임 참조: 생성물이 P0-4/5 번들 매니페스트(`common/header.jsp` 상당)를 include/참조하도록 배선(로컬 복사본만 참조 — 4대 축 3번). 절대 외부 CDN 참조 금지.
- [ ] **스코프 경계 주석**: 각 `.ftl` 상단에 "P4 MVP: MGMT_LIST_DETAIL+TABLE_VIEW 1뷰. 다른 뷰/모듈은 P5" 명시. searchArea/listToolbar/detail 슬롯은 존재 시에만 조건부 렌더(`<#if>`), 미배치 시 깨지지 않게.
- **AC**:
  - §6 예시 DEFINITION_JSON을 P4-2 컨텍스트로 넘겼을 때 3종(6~9파일: jsp/js/css × shell/list/tableView) 렌더 결과가 **문법상 유효한 JSP/JS/CSS**(스크립트릿 0, 배너 0).
  - TABLE_VIEW props(columns 3행/selectMode=checkbox/pagingYn 등)가 산출물에 반영됨.
  - 🔒 인젝션 테스트: props에 악성 문자열 주입 시 산출물이 `<script>` 실행 코드/템플릿 인젝션(`${}`/`<#...>`)으로 **평가되지 않음**(전부 이스케이프된 리터럴). reviewer 검수 PASS(blocker/high 0).
  - `common/header.jsp` 매니페스트(jQuery 3.7.1 등) 로컬 참조, 외부 CDN 0.
- **의존**: P4-0(아티팩트 매핑/이스케이프 정책), P4-2(컨텍스트/이스케이퍼), P0-4/5(번들 매니페스트). P4-4가 이 템플릿을 렌더·파일화.

---

## P4-4. 🔒 생성 파이프라인 오케스트레이션 + 원자적 파일쓰기 — coder → reviewer (보안 관문 필수) ✅ 완료

> **완료 메모(2026-07-02)**: reviewer 보안검수 PASS(blocker/high 0). `ScreenGenerator.generate(screenId)` + `AtomicFileWriter.write(safeAbs, canonicalRoot, content)` 구현. 모든 쓰기 `resolveSafeWritePath` 경유, relativePath는 정적세그먼트+role+stem만 조립(자유문자열 0), TEMPLATE_KEY 맵 조회. 심볼릭링크 하드거부(NOFOLLOW 이중확인), **계약 §5.1 쓰기-직전 재확인**: `createDirectories` 직후 `parent.toRealPath().startsWith(canonicalRoot)` 하드검증으로 순수 TOCTOU 차단(`AtomicFileWriter.java:91-104`, canonicalRoot=`targetRoot.toRealPath()` 1회 산출 `ScreenGenerator.java:114-116`). 임시파일→ATOMIC_MOVE(폴백 REPLACE_EXISTING), `.bak-{ts}` 백업, PARTIAL/FAIL/SUCCESS 판정. 전체 스위트 59 tests / 0 failures / 2 skipped(심볼릭링크 권한), BUILD SUCCESS, WAR OK.

**P4 보안의 심장.** DEFINITION_JSON → (P4-2)컨텍스트 → (P4-1/P4-3)렌더 → **PathSafetyService 경로해석 → 원자적 파일쓰기** → 결과 취합. **임의 파일쓰기·경로탈출·템플릿 인젝션이 전부 걸리는 태스크** → reviewer 통과가 AC.

- [x] `gen.pipeline.ScreenGenerator`(@Service) — `GenResult generate(Long screenId)`: 화면+프로젝트 로드(404 처리) → P4-2 컨텍스트 → 아티팩트 목록별(P4-0 매핑) 렌더(P4-3) → **각 파일마다** `PathSafetyService.resolveSafeWritePath(targetRoot, relPath)`로 안전 절대경로 획득 → 원자적 쓰기 → 결과(파일목록·성공/실패) 반환.
- [x] 🔒 **원자적 파일쓰기** `gen.pipeline.AtomicFileWriter`(@Component): **임시파일(같은 디렉토리)→`Files.move`(ATOMIC_MOVE, 실패시 REPLACE_EXISTING 폴백)**. 부모 디렉토리 생성도 PathSafetyService 통과 경로 하위로만. 인코딩 UTF-8 고정.
- [x] 🔒 **덮어쓰기/백업 정책**(P4-0 확정 정책 구현): 기존 파일 존재 시 `.bak`(타임스탬프) 백업 후 덮어쓰기(기본) 또는 정책값. **경로안전 통과 실패 파일은 즉시 실패 처리**(부분성공 시 이미 쓴 파일 롤백 여부·PARTIAL 판정 명확화).
- [x] 🔒 **모든 relativePath는 정적 세그먼트 + 화이트리스트 stem으로만 조립**. props값/instanceId/label 등 자유문자열을 경로에 **절대 미포함**(P4-0 §경로안전). TEMPLATE_KEY→템플릿 파일 해석도 맵 조회(문자열 조립 금지).
- [x] 부분실패 처리: 일부 파일 렌더/쓰기 실패 시 `RESULT_CODE=PARTIAL`, 전량 실패 `FAIL`, 전량 성공 `SUCCESS`. 실패 사유 로깅. GEN_HIST 기록은 P4-5.
- **AC**:
  - screenId 투입 → 타겟 폴더(테스트용 임시 루트)에 3종 파일 실제 생성, 내용이 P4-3 렌더 결과와 일치.
  - 🔒 **경로탈출 차단 테스트**: relativePath에 `..`/절대경로/드라이브/UNC/화이트리스트밖 확장자 주입 시 `PathSafetyException`으로 차단, **타겟 루트 밖 파일 0**(테스트가 루트 밖에 아무것도 안 씀을 검증). 심볼릭링크 탈출도 차단.
  - 🔒 원자성: 쓰기 중 실패 시 부분/깨진 파일이 최종 경로에 남지 않음(임시→rename). 기존 파일 `.bak` 백업 동작.
  - 🔒 **reviewer 보안검수 PASS(blocker/high 0)**: (1)모든 쓰기 PathSafetyService 경유, (2)경로/파일명에 자유문자열 미포함, (3)템플릿 인젝션 방지(P4-2/3 이스케이프 경유·모델 데이터만), (4)원자적 쓰기·백업 정확성, (5)확장자 화이트리스트. **이 태스크는 reviewer PASS 없이는 완료 불가.**
- **의존**: P4-1(렌더러), P4-2(컨텍스트), P4-3(템플릿), P1-1(PathSafetyService), P3-1(screen 조회), P1-2(project 조회). P4-5가 결과를 이력화.

---

## P4-5. GEN_HIST 기록 + 생성 트리거 API/UI ("생성" 버튼) — coder ✅ 완료

> **완료 메모(2026-07-02)**: reviewer 보안검수 PASS(blocker/high 0). `gen.hist` 패키지 신설 — `GenHistMapper`(IF+XML, `${}`/`SELECT *` 0건, `FILE_LIST_JSON` `::jsonb`(쓰기)/`::text`(읽기) 왕복, GEN_AT DB DEFAULT), `GenHist`(도메인, `@JsonRawValue`), `GenerationService`(파사드 — 보안 심장 `ScreenGenerator` 무수정으로 감쌈, Jackson ArrayNode/ObjectNode 직렬화로 FILE_LIST_JSON 조립, 성공/부분/실패 모두 기록), `GenController`(`POST /api/screens/{id}/generate`, `GET /api/screens/{id}/gen-history` — 저장과 별도 엔드포인트). 스튜디오 "생성" 버튼(녹색, 저장과 구분) + 결과/이력 패널(`textContent`/`createElement`만, innerHTML 0), dirty 시 confirm 선저장 유도. reviewer medium 1건(파일쓰기 후 insert 실패 시 감사이력 누락) 강화 반영: insert 실패 시 파일 잔존 사실을 ERROR 로그로 남기고 재던짐(`GenerationService.java:74-84`). 전체 스위트 64 tests / 0 failures / 2 skipped(+5 신규), BUILD SUCCESS, WAR OK.

생성 결과를 `TB_FRG_GEN_HIST`에 남기고, 스튜디오에 **"생성" 버튼**(P3 "저장"과 구분)을 추가한다. 저장=DB(P3-6b), 생성=파일쓰기(P4).

- [x] `gen.hist.*`: `GenHistMapper`(IF+XML) — INSERT(`SCREEN_ID`/`FILE_LIST_JSON::jsonb`/`RESULT_CODE`, `GEN_AT` DEFAULT) + 화면별 이력 조회(`<sql id>` 컬럼명시, `SELECT *` 0, `#{}` 바인딩). `FILE_LIST_JSON`은 쓴 파일 상대경로/해시 목록(Jackson으로 안전 직렬화, 문자열조립 0).
- [x] `ScreenGenerator`가 P4-4 결과를 GEN_HIST에 기록(성공/부분/실패 모두 이력 남김 — 실패도 감사 대상). *(구현: 보안 심장 무수정 유지 위해 `GenerationService` 파사드가 기록)*
- [x] **생성 API**: `POST /api/screens/{id}/generate` → `ScreenGenerator.generate` → 응답 `{resultCode, files:[...], genHistId, genAt}`. 실패 시 규약 오류 응답(`ApiExceptionHandler`). **저장(PUT definition)과 별도 엔드포인트**.
- [x] **생성 이력 조회 API**: `GET /api/screens/{id}/gen-history`(최신순, 메타만).
- [x] **스튜디오 UI**: P3 `studioApp.js`/`list.jsp`에 "생성" 버튼 + 결과 표시(생성 파일 목록·성공/실패, `textContent`만). "저장"과 시각·동작 구분(저장 안 된 dirty 상태에서 생성 시 경고/선저장 유도 정책). 상단 "생성 이력" 표시 슬롯(기획서 7장).
- **AC**:
  - `POST /api/screens/{id}/generate` → 파일 생성 + GEN_HIST 1행(RESULT_CODE 정확, FILE_LIST_JSON에 쓴 파일 목록). `GET .../gen-history`로 조회됨.
  - 실패/부분 케이스도 이력에 기록(FAIL/PARTIAL). 없는 screenId 404.
  - 매퍼 `${}`/`SELECT *` 0건, `FILE_LIST_JSON` `::jsonb`+`#{}`, 무가공 왕복.
  - 스튜디오 "생성" 버튼 동작, 결과 표시 `textContent`만(XSS 0), 저장 버튼과 구분. `/admin/studio` 회귀 0.
- **의존**: P4-4(파이프라인 결과), P0-3(GEN_HIST 테이블), P3-3/P3-6b(스튜디오 셸·저장버튼). UI는 studioApp 공개 API 재사용(허브 무변경 지향).

---

## P4-6. 공통코드 2층위 최소 처리 + stub 폴더 — coder (reviewer 🔒 경로안전 재확인) ✅ 완료

> **완료 메모(2026-07-02)**: 🔒 경로안전 재확인 PASS(blocker/high 0). reviewer 에이전트가 세션 한도로 판정 전 중단되어 **오케스트레이터(Claude)가 직접 검증**: (1)모든 타겟 쓰기 3곳(`ScreenGenerator`/`RuntimeSyncer:158`/`StubGenerator:99`) 전부 직전 `resolveSafeWritePath` + `AtomicFileWriter.write(safeAbs, canonicalRoot, content)` 경유 — `Files.write`/`Files.copy`는 `AtomicFileWriter` 내부(116/161)에만 존재, 두 신규 클래스에 직접쓰기 0(grep 확인), (2)canonicalRoot=`targetRoot.toRealPath()` 1회 산출값을 런타임/stub에 전달 → 화면 3종과 동일한 계약 §5.1 쓰기-직전 재확인 적용, (3)`TemplateContextBuilder.PACKAGE_BASE_REGEX="^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)*$"` 앵커드 하드검증을 점→슬래시 변환 **전에** 수행(위반 시 컨텍스트 구성 하드실패·원문 미노출), (4)경로 자유문자열 0(런타임=정적 상수 30개 목록, stub=정적세그먼트+검증 packageBase(점→슬래시)+검증 stem+정적접미사 Controller/Mapper), (5)확장자 화이트리스트 내(런타임 js/css, stub java/xml), (6)런타임 skip은 `Files.exists(NOFOLLOW_LINKS)`로 안전경로에 대해서만(재복사 0). 기존 `generateOne`/`buildRelativePath` 무변경. 공통추출(2층위)·버전관리 P6 이관 명시. 전체 스위트 69 tests / 0 failures / 2 skipped(+5 신규), BUILD SUCCESS, WAR OK.

기획서 6장 공통코드 2층위 + 10장 "stub 폴더". **P4는 최소 정책만**(심화·버전관리·공통추출 정교화는 P6).

- [x] **런타임 공통(1층위) — 1회 동기화·중복금지**: 번들 런타임(commonList* 등, header.jsp 매니페스트)을 타겟 `TARGET_ROOT_PATH` 하위에 **1회 복사·동기화**(이미 존재하면 재복사 skip 또는 버전비교 — **P4는 "존재하면 skip" 최소 정책**, 버전관리는 P6). 화면 반복 생성 시 런타임 중복 생성 0. 🔒 복사 경로도 PathSafetyService 통과.
- [x] **화면 반복패턴(2층위) — 최소만**: P4 MVP는 단일 화면이라 공통추출은 **정책 명시만**(공통 파셜/상수 추출은 P6). P4는 per-screen 산출로 두되, 문서에 "P6에서 공통추출" 표시.
- [x] **stub 폴더/파일**(기획서 10장 "3종+stub 폴더"): Controller/Mapper 위치에 **빈 stub**(패키지 폴더 + 최소 스텁 파일 `{Stem}Controller.java`/`{Stem}Mapper.java`/`.xml` 골격 — 실 로직 없음, TODO 주석). 🔒 stub 경로도 PathSafetyService 통과(`java`/`xml` 확장자 화이트리스트 내), 패키지 폴더는 `PACKAGE_BASE`(정규식 검증) + 정적 세그먼트로만 조립.
- **AC**:
  - 동일 화면 2회 생성 시 런타임 공통 파일 중복/재복사 0(skip 확인), 화면 3종은 재생성(백업 후).
  - stub 파일이 stub 위치에 생성(빈 골격), java/xml 확장자 화이트리스트 통과.
  - 🔒 reviewer 재확인 PASS: 런타임 복사·stub 경로 전부 PathSafetyService 경유, PACKAGE_BASE 조립에 자유문자열 미포함, 확장자 화이트리스트 준수. 공통추출 P6 이관 명시.
- **의존**: P4-4(파이프라인), P1-1(PathSafetyService), P0-4/5(번들 런타임 원본). MVP 완주(P4-7)에는 stub/런타임동기화가 필수는 아니나 "3종+stub"(10장 정의) 충족용.

---

## P4-7. 🔒 골든파일 스냅샷 검증 + 경로안전 테스트 — tester (reviewer 결과확인) ✅ 완료 — **P4 MVP 완주**

> **완료 메모(2026-07-02)**: AC 전 항목 충족, 전체 스위트 **75 tests / 0 failures / 0 errors / 2 skipped(심볼릭링크 권한)**, BUILD SUCCESS(오케스트레이터 독립 재실행으로 재확인). 신규 테스트 3종 — `GoldenSnapshotTest`(7종 정규화 비교 + 안전성 자동단언 + 결정성 2회동일), `InjectionGoldenTest`(이스케이프 산출 golden 고정 + 무결성 재단언), `PipelinePathSafetyTest`(UNC·유닉스절대·중간우회 3벡터, 루트 밖 파일 0). 골든 리소스 `golden/mgmtListDetail_tableView/`(7종) + `golden/injection_mgmtListDetail/`(3종) + `golden/README.md`(결정성·안전성·갱신 절차). 골든 안전성 육안+자동 확인: 배너 0(JWORKS/COPYRIGHT grep 0), 스크립트릿 0(`<%`/`<%=`/`<%!` 부재, 지시자 `<%@`만), jQuery 3.7.1(매니페스트 경유), JS 네임스페이스+IIFE+`__defined`, 이스케이프 확인(`</script>`→엔티티(JSP)/`\x3C\/`(JS), `${7*7}`/`${9*9}` 미평가, U+2028 0건, CSS 위반토큰 드롭). 프로덕션 코드 버그 0(gen 패키지 무수정).

기획서 8장 tester "생성 엔진 검증(골든파일 스냅샷), 경로안전 테스트" · 9장 golden-test 스킬. **P4 MVP 완료 판정의 최종 관문.**

- [x] **골든 픽스처**: 고정 입력(§6 예시 상당 DEFINITION_JSON + 고정 프로젝트 메타)에 대한 **기대 산출물(golden)** 7종을 `src/test/resources/golden/mgmtListDetail_tableView/`에 커밋. 결정성 보장(타임스탬프·랜덤·백업 배제, 개행 정규화).
- [x] **스냅샷 비교 테스트**: 생성기를 임시 타겟 루트로 돌려 산출 파일을 golden과 **정규화 비교**(CRLF→LF). 불일치 시 실패 + 진단. golden 갱신 절차 문서화(README + 클래스 javadoc).
- [x] 🔒 **경로안전 테스트 세트**: `..`/절대경로/드라이브(`C:`)/UNC/화이트리스트밖 확장자/심볼릭링크 탈출 주입 시 전부 차단되고 **타겟 루트 밖에 파일이 하나도 생기지 않음**을 검증(격리된 임시 루트 사용, 테스트 후 클린업). PathSafetyService 단위테스트(P1-1 존재분)와 파이프라인 통합 레벨(신규 `PipelinePathSafetyTest`) 양쪽.
- [x] 🔒 **인젝션 회귀 테스트**: props 악성 문자열(§P4-2/3) 주입 산출물이 이스케이프됨을 golden에 고정(회귀 감지).
- [x] **결정성/멱등**: 같은 입력 2회 생성 → 같은 산출(백업 제외). *(GEN_HIST 이력 멱등은 P4-5 GenerationServiceTest 소관.)*
- **AC**:
  - 골든 스냅샷 테스트 통과(MGMT_LIST_DETAIL+TABLE_VIEW 3종). CI/로컬 재현 가능.
  - 🔒 경로안전 테스트 전 케이스 통과(루트 밖 파일 0). 인젝션 회귀 golden 고정.
  - 🔒 reviewer 결과확인: 골든이 실제로 안전한 산출물(배너 0, 스크립트릿 0, 이스케이프됨)인지, 경로안전 커버리지 충분한지 확인 PASS.
- **의존**: P4-3(템플릿), P4-4(파이프라인), P4-2(이스케이프), P1-1(PathSafetyService). **P4 MVP 완주 = 이 태스크 통과.**

---

## 의존 순서

```
P4-0(생성 파이프라인 계약, 🔒신뢰경계·경로안전) ──┬─→ P4-1(FreeMarker 엔진 배선)
   (최우선·전 태스크 계약)                        │
                                                  ├─→ P4-2(TemplateContext + 🔒이스케이프 유틸)
                                                  │        │
                                                  ├─→ P4-3(FreeMarker 템플릿 3종, 🔒인젝션) ◄─┘
                                                  │        │
                                                  └────────┴─→ P4-4(🔒파이프라인+원자적 파일쓰기, reviewer 관문)
                                                                    │
                                                        ┌───────────┼───────────┐
                                                        ▼           ▼           ▼
                                              P4-5(GEN_HIST+생성 API/버튼)  P4-6(공통2층위+stub, 🔒)
                                                        │           │           │
                                                        └───────────┴───────────┴─→ P4-7(🔒골든+경로안전 테스트)
                                                                                       = P4 MVP 완주 판정

[재사용 선행(완료): P1-1 PathSafetyService(파일쓰기 최종방어) / P1-2 project(TARGET_ROOT/PACKAGE/basePath) /
 P3-1 screen+DEFINITION_JSON 조회 / P2-1 TEMPLATE_KEY 시드 / P0-3 GEN_HIST·MODULE_TYPE 테이블 /
 P0-4·5 번들 런타임·header.jsp 매니페스트 / P3-2 DEFINITION_JSON §5 신뢰경계 / ApiExceptionHandler·NotFoundException]
[경계: 다른 아키타입/모듈 완전생성·공통추출 정교화·런타임 버전관리·라운드트립 = P5/P6]
```

- **P4-0이 전 태스크의 계약**이라 최우선(P3-2 선례). 확정 전 P4-2/3/4 착수 시 재작업 위험.
- P4-1(엔진)·P4-2(컨텍스트/이스케이프)는 계약 후 병렬 가능. P4-3(템플릿)은 P4-2 이스케이퍼에 의존.
- **P4-4가 보안 심장** — P4-1/2/3을 통합하며 reviewer 관문 필수. P4-5/6은 P4-4 위에서 병렬.
- **P4-7이 MVP 완주 판정** — 골든·경로안전 통과로 페이즈 종료.
- 🔒 관문 배치: **P4-0(계약)·P4-2(이스케이프)·P4-3(인젝션)·P4-4(파일쓰기 핵심)·P4-6(경로 재확인)·P4-7(골든/경로안전)** — 임의 파일쓰기 페이즈답게 reviewer를 다층 배치.

## 이번 스프린트 착수 후보 (첫 2개)

1. **P4-0** 생성 파이프라인 계약 확정 + 🔒신뢰경계·경로안전 (pm/template-author → reviewer) — 나머지 전부의 계약이라 최우선. DEFINITION_JSON §5 이스케이프 정책을 산출 3문맥(HTML/JS/CSS)으로 구체화하고 경로안전 계약을 명문화. 재작업 방지의 핵심.
2. **P4-1** FreeMarker 엔진 배선 (coder) — 계약이 서는 즉시 착수 가능. pom 의존성 추가 + 코드생성 전용 Configuration(빌더 JSP 뷰와 분리). 이후 모든 렌더 태스크의 토대이며 저위험(보안면 작음)이라 병렬 선행에 적합.
