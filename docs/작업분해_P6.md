# J-FORGE 작업분해 — P6 공통추출·런타임 버전관리·round-trip·골든 심화

> **PM 산출물(2026-07-03).** 로드맵 110행 최종 페이즈 **P6**. 선행: P0~P4(✅), **P5 MVP(✅ Card/Tree/Form)**, **P5.5a/b(✅ Detail+AssociateTabs)**, **P5-5c(✅ DUAL_LAYOUT)**, P5-5d(설계상 해소). 소스오브트루스: [기획서](기획서_v0.1.md) 0장 4대 설계축(특히 3번 번들 런타임 복사·참조) · 8장(골든) · 로드맵 110행. 확장 대상 계약: [생성엔진_계약.md](생성엔진_계약.md) §0~§10(불변, add-only만).
>
> **P6 성격**: P5까지는 "타입 확장"(뷰/모듈/아키타입 add-only). P6는 **파이프라인 성숙도**(반복생성 효율·버전 안전·재편집·회귀망) 4축이다. 여전히 **보안 심장(ScreenGenerator/AtomicFileWriter/PathSafetyService) 무수정** 원칙 유지 — P6도 이들을 호출/보강만 한다(RuntimeSyncer는 P4-6/P5에서 이미 P6 확장 예고: 38행 "런타임 버전 비교/갱신·2층위 공통추출은 P6").
>
> 상태 표기: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료. 보안 태스크(🔒)는 reviewer 통과가 AC.

---

## P6 범위 요약 / 4축

| 축 | 무엇 | 현재 상태(P5까지) | 난이도 | 리스크 |
|---|---|---|---|---|
| **P6-1 런타임 버전관리** | RuntimeSyncer가 "존재하면 skip" → **버전 비교 후 갱신/보존** | `RuntimeSyncer`(38·143행): 존재 시 무조건 skip. `ForgeProject.runtimeVer` 필드 존재하나 미사용 | 저~중 | 저(add-only, 기존 skip은 기본값) |
| **P6-2 공통추출(2층위)** | 화면 반복패턴(공통 파셜·상수·베이스)을 화면당 중복 산출 → **1회 공통 산출** | P4-6: 1층위(번들 런타임)만 복사. 2층위(화면군 공통)는 미구현(ScreenGenerator 156행 "P6 이관") | 중~고 | 중(무엇을 추출할지 설계 필요) |
| **P6-3 round-trip(재오픈)** | 생성된 화면을 **스튜디오로 재오픈**해 재편집 | `ForgeScreen.definitionJson` 영속(저장). 스튜디오 로드 경로 존재 여부·왕복 무손상 확인 필요 | 중 | 중(데이터 모델·왕복 결정성) |
| **P6-4 골든 심화** | 조합·엣지 골든 확대 + 골든 유지보수 절차 | P4-7/P5-6/P5.5/P5-5c 골든: 뷰 단독·detail·dual 각 단위. 조합(searchArea+toolbar+detail+뷰전환)·엣지 미커버 | 저 | 저(테스트만, 프로덕션 무변경) |

**권장 착수 순서(로드맵 순 + 리스크 순 정합)**: **P6-1(런타임 버전관리) → P6-4(골든 심화) → P6-2(공통추출) → P6-3(round-trip)**. 근거: P6-1/P6-4는 저risk·독립·즉시가치(버전 안전망·회귀망). P6-2/P6-3은 설계 포크가 있어 사용자 확인이 유익(아래 각 태스크 "스코프 결정" 참조).

---

## P6-0. 🔒 P6 계약 부록 스텁 (필요 시) — pm/reviewer

- [ ] P6-1~4 중 **계약(생성엔진_계약.md) 변경이 필요한 것만** §11 add-only 부록으로 고정. 예: 런타임 버전관리의 갱신 정책(언제 덮어쓰기/백업), 공통추출의 산출 위치·경로안전 재확인, round-trip의 DEFINITION 왕복 계약. **§0~§10 불변.**
- **AC**: 변경 필요한 축만 부록화(불필요하면 스킵), P4 계약과 모순 0, reviewer PASS.
- **의존**: 없음(각 태스크 착수 직전 해당 부분만).

---

## P6-1. 런타임 버전관리 (RuntimeSyncer 버전 비교·갱신) — coder (reviewer 🔒 경로안전·덮어쓰기 검수) ✅ 완료

> **완료 메모(2026-07-03)**: 계약 **§11 부록**(add-only) 확정. `RuntimeSyncer`에 세트 버전 `RUNTIME_SET_VERSION="1.0.0"` + 타겟 마커 `static/forge-runtime.xml`(확장자 xml=§4.1 화이트리스트 내, dotfile 불가라 `.xml`) 도입. sync가 마커 버전을 읽어 **FRESH(마커 없음→파일별 존재-skip+스탬프) / SKIP(==) / UPDATE(구버전→.bak 백업 후 덮어쓰기+마커 상향) / DOWNGRADE(상위→차단)** 1회 결정. `syncOne(…, forceOverwrite)` 확장, `readMarkerVersion`/`writeMarker`/`compareVersions`(점 구분 정수 비교) 추가. 🔒 마커 읽기/쓰기·복사·갱신 전부 `resolveSafeWritePath`(+`AtomicFileWriter`) 경유, 복사 대상은 여전히 정적 상수 목록만. **마커는 GenResult 결과 제외**(화면/런타임 산출 카운트 불변 → 기존 "2회차 재복사 0" 테스트 계약 보존). 보안 심장 무수정. `RuntimeSyncerVersionTest` 5종(신규/동일/구버전 갱신+백업/상위 차단/compareVersions). **전체 125 tests / 0 failures / 2 skipped, BUILD SUCCESS**(+5).

현재 `RuntimeSyncer.syncOne`(143행)은 대상이 존재하면 **무조건 skip**한다. 번들 런타임이 갱신되면(예: commonSection.js 버그픽스) 기존 타겟은 낡은 버전을 유지한다. P6-1은 **버전 비교 후 필요 시 갱신**(백업 후 덮어쓰기)하도록 확장한다.

- [ ] **버전 매니페스트**: 번들 런타임의 버전(예: `runtimeVer` = 프로젝트 메타 `ForgeProject.runtimeVer`, 또는 런타임 세트 버전 상수)을 타겟에 **버전 마커 파일**(예: `static/.forge-runtime-version`)로 기록. sync 시 타겟 마커 vs 현재 번들 버전 비교.
- [ ] **갱신 정책**: 마커 없음/구버전 → 런타임 재복사(AtomicFileWriter가 `.bak` 백업 후 덮어쓰기, 계약 §5). 동일 버전 → skip(현행 유지). **다운그레이드 금지**(타겟이 더 높으면 skip+경고).
- [ ] 🔒 **경로안전 불변**: 갱신도 예외 없이 `resolveSafeWritePath`+`AtomicFileWriter` 경유(RuntimeSyncer 무보안회귀). 복사 대상은 여전히 정적 상수 목록만.
- [ ] **결과/로그**: sync 결과에 copied/updated/skipped/downgraded 구분. GEN_HIST 연동은 선택.
- **스코프 결정(사용자)**: 버전 단위 = (a) 런타임 세트 단일 버전(간단, 권장) vs (b) 파일별 버전(정밀, 복잡). MVP는 (a).
- **AC**: 신규 프로젝트(마커 없음) 전량 복사; 동일버전 재생성 시 skip(재복사 0); 번들 버전 상향 시 백업 후 갱신; 다운그레이드 차단. 🔒 reviewer PASS(경로안전·정적목록·백업정책), 기존 RuntimeSyncer 테스트 무회귀.
- **의존**: P4-6 RuntimeSyncer. 저risk 우선 착수 후보.

---

## P6-2. 공통추출 (2층위 화면군 공통 파셜·상수) — coder + template-author (reviewer 🔒 경로안전) ✅ 완료

> **완료 메모(2026-07-03)**: 계약 **§12 부록**(add-only) 확정. **재분석으로 추출 대상 확정**: per-screen 뷰 CSS(`{stem}ListTableView.css` 등)·`{stem}Detail.css`·`{stem}.css`(dual)는 (a) 고정 셀렉터(stem·props 0)라 화면 간 **완전 중복**이고 (b) 뷰/상세 CSS는 shell/list 어디서도 **link 안 돼 실제 적용 안 되던 결함**까지 있었다(사용자가 실산출 검증 중 지적한 경로버그와 별개의 죽은 파일 문제). → 이 고정 규칙 전부를 번들 **`static/css/admin/common/commonScreenLayout.css` 1벌**로 추출, header.jsp 매니페스트가 link(모든 shell이 header include) → **중복 제거 + 결함(미적용) 해소**. RuntimeSyncer가 1회 복사(`RUNTIME_SET_VERSION` 1.0.0→**1.1.0**, §11 갱신정책으로 기존 타겟도 자동 갱신). `GenArtifacts`에서 뷰4종 `...Css`·`detailCss`·`dualCss` 스펙 제거(뷰/Detail/DUAL 각 3종→2종), `dualLayout/shell.ftl` `<link {stem}.css>` 제거. **`{stem}List.css`(#{stem}-list 스템 스코프)는 진짜 per-screen이라 유지.** 은퇴 템플릿 6종은 미사용(파일 잔존). 골든: 뷰/상세/듀얼 per-screen CSS 골든 삭제(10파일) + dual shell 골든 갱신(link 제거) + 12개 테스트 file-map/assertion 정합. 보안 심장 무수정. **전체 129 tests / 0 failures / 2 skipped, BUILD SUCCESS.**

> **(원 보류 분석, 참고)**: 최초엔 **stem 박힌 파일만 보고** 추출 대상이 없다고 판단했으나, 사용자 실산출 검증에서 고정 CSS의 중복·미링크를 발견해 재분석 → 위와 같이 실제 추출 대상 확정. 근거: (1) 화면당 산출물(`{stem}.jsp`·`{stem}List.js`(NS `MagicIAM_JS{Domain}{Role}`)·뷰/상세 파일·stub 컨트롤러/매퍼)은 전부 **stem/role/NS/props가 baked-in**이라 화면 간 *구조*는 같아도 *내용*이 달라 단일 공유 파일로 바이트 추출 불가(각 화면이 자기 네임스페이스 배선을 필요로 함 — 축소 불가한 per-screen). (2) 진짜 공유 코드(commonList/commonSection/commonList*View·jQuery·jworks)는 **이미 번들 런타임(1층위)으로 추출**되어 `RuntimeSyncer`가 화면 반복생성 시 1회만 복사(중복 0). → **1층위가 공유 코드를 이미 커버, 2층위는 축소 불가한 per-screen 배선뿐**이므로 2층위 공통추출은 **투기적**(speculative)이다. **결정**: 미래에 "화면군 공통 실로직"(예: role별 공유 베이스 컨트롤러의 실제 공통 메서드, 도메인 공통 JS 상수 세트)이 **실제로 등장**하면 그때 최소 1종부터 추출(경로안전·1회산출·버전정책 P6-1 재사용). 현 시점 강행은 무가치. **프로덕션 무변경.**

P4-6 주석(ScreenGenerator 156행)이 예고한 **"화면 반복패턴(2층위) 공통추출"**. 화면을 여러 개 생성하면 화면군(같은 role/도메인)에서 반복되는 조각(공통 상수 JSP, 베이스 컨트롤러, 공통 CSS 토큰 등)이 화면마다 중복 산출된다. P6-2는 이를 **1회 공통 산출 + 화면은 참조**로 바꾼다.

- [ ] **추출 대상 식별**: 무엇이 "2층위 공통"인가 확정 — 후보: (i) 화면 공통 JS 상수/유틸(도메인별), (ii) 공통 레이아웃 파셜(header/footer 조립 조각), (iii) 공통 CSS 변수. 번들 런타임(1층위, RuntimeSyncer)과 **중복 금지**.
- [ ] **산출·참조 규칙**: 공통 산출물의 타겟 경로(정적 세그먼트+role만, 계약 §4.3)·파일명 규칙 확정. 화면 shell이 공통을 include/link. **1회 산출(존재 시 skip 또는 P6-1 버전정책 재사용)**.
- [ ] 🔒 경로안전·정적매핑: 공통 산출도 `resolveSafeWritePath`+`AtomicFileWriter`, 경로 자유문자열 0.
- **스코프 결정(사용자)**: 추출 범위(위 i/ii/iii 중 어디까지). 과도하면 리스크↑ → **최소 1종부터**.
- **AC**: N개 화면 생성 시 공통 조각이 1회만 산출, 화면은 참조. 산출물 문법 유효·MagicIAM 규약. 🔒 경로안전 reviewer PASS. 골든으로 "공통 1회+화면 참조" 고정.
- **의존**: P6-1(버전/skip 정책 재사용 가능). 설계 포크 있음 → 착수 전 스코프 확정.

---

## P6-3. round-trip 재오픈 (생성 화면 → 스튜디오 재편집) — coder (reviewer 검수) ✅ 완료(검증)

> **완료 메모(2026-07-03)**: **재오픈 기능은 P3에서 이미 구현됨** — `studioApp.loadScreen()`(studioApp.js:165)이 `GET /api/screens/{id}`의 `definitionJson`(`@JsonRawValue` 원문)을 파싱해 캔버스 하이드레이트(:170), 화면 드롭다운 선택 시 로드(:472), 저장은 `PUT /api/screens/{id}/definition`이 **raw String 무가공 영속**(ForgeScreenController:69 "Jackson 재직렬화로 인한 훼손 방지") → 저장→재오픈 왕복이 **DEFINITION_JSON 바이트 보존**. 즉 스코프 결정 (a)(DEFINITION 재로딩)가 이미 충족. P6-3은 그 위에서 **"재오픈→재생성 무손상"을 풀조합 화면으로 고정**: `RoundTripDeterminismTest`가 동일 DEFINITION(저장소 원문)으로 두 독립 타겟에 생성 → 산출 10종 **바이트 동일** 단언(숨은 상태·비결정성 0). GoldenSnapshotTest TABLE_VIEW "2회 생성 동일"을 풀조합으로 확장. **역파싱(산출 JSP→DEFINITION 복원, 옵션 (b))은 불필요**(DEFINITION이 정본). 프로덕션 무변경. **BUILD SUCCESS**(+1).

생성된 화면을 **다시 스튜디오로 열어 재편집**. `ForgeScreen.definitionJson`이 이미 영속되므로, 핵심은 **저장된 DEFINITION_JSON을 스튜디오 캔버스로 재로딩**하고 재편집→재저장→재생성이 **무손상 왕복**임을 보장하는 것.

- [ ] **재오픈 경로 확인/구현**: 스튜디오 화면 편집 진입 시 기존 `DEFINITION_JSON`을 로드해 팔레트/프리뷰/속성패널에 복원. (studioApp.js/propsPanel.js 로드 경로 점검 — 이미 되는지, 갭이 있는지.)
- [ ] **왕복 무손상**: 로드→무변경 저장 시 DEFINITION_JSON 바이트/의미 동일(키 순서·기본값 처리 결정성). 재생성 시 골든 동일.
- [ ] **버전/스키마 진화**: `schemaVersion` 하위호환(현재 1). 미지원 slot/module은 forward-compat(스튜디오에서 경고 표시).
- **스코프 결정(사용자)**: round-trip 범위 = (a) DEFINITION 재로딩·재편집(스튜디오 왕복, 권장) vs (b) **산출 JSP/JS 역파싱→DEFINITION 복원**(고난이도, 일반적으로 불필요 — DEFINITION이 정본이므로). 권장 (a).
- **AC**: 기존 화면 재오픈 시 캔버스 정확 복원, 무변경 왕복 무손상, 재생성 골든 동일. reviewer PASS.
- **의존**: P3 스튜디오(팔레트/프리뷰/속성패널), DEFINITION_JSON 영속. 설계 확인 필요.

---

## P6-4. 골든 심화 (조합·엣지 골든 + 유지보수 절차) — tester (reviewer 결과확인) ✅ 완료(핵심)

> **완료 메모(2026-07-03)**: `FullCombinationGoldenSnapshotTest` — **풀조합 MGMT**(searchArea+listToolbar+listArea(TABLE_VIEW)+detailToolbar+detailBasic+detailTabs 6슬롯)을 한 화면에 배치한 산출 **10종 골든**(`golden/fullCombination`) + **전 슬롯·전 문맥 인젝션 골든**(`golden/injection_fullCombination`, 악성 props → 이스케이프 박제). 슬롯 상호작용(list.ftl search/toolbar 조건부 + 뷰 분기 + shell with-detail + Detail 동시산출)을 단위 골든이 못 잡는 지점까지 회귀 고정. 육안 검증: search `name="st&quot;onx"`/`aria-label="&lt;script&gt;"`, toolbar `${7*7}` 리터럴(미평가), detail 미허용 type→text 수렴, cssToken 위반 토큰 드롭, tab `id="f&quot;&gt;"` — 전 문맥 이스케이프 확인. `golden/README.md`에 P5.5(detail)·P5-5c(dualLayout)·P6-4(fullCombination) 골든 디렉터리 + 갱신 커맨드 추가(유지보수 정례화). **엣지(빈슬롯·forward-compat 스킵·다중 listArea[0])는 기존 단위 테스트**(`ScreenGeneratorTest`/`ListAreaViewBranchTest`)로 커버 — 조합 골든이 핵심 갭. **전체 128 tests / 0 failures / 2 skipped, BUILD SUCCESS**(+3).

단위 골든(뷰 단독·detail·dual)은 P4-7/P5-6/P5.5/P5-5c에서 확보됐다. P6-4는 **현실적 조합·엣지**로 회귀망을 넓히고 골든 유지보수를 정례화한다.

- [ ] **조합 골든**: searchArea+listToolbar+listArea(뷰)+detailToolbar+detailBasic+detailTabs를 **한 화면에** 배치한 풀조합 MGMT 골든. DUAL_LAYOUT 좌=목록/우=상세 조합.
- [ ] **엣지 골든**: 빈 슬롯·0필드·최대필드·유니코드/개행 props·미지원 moduleTypeCode(forward-compat 스킵)·다중 listArea([0]만) 등 경계값.
- [ ] 🔒 **인젝션 회귀 확대**: 조합 화면 전 문맥(HTML텍스트/속성/JS/CSS)에서 악성 props 이스케이프 골든 고정.
- [ ] **유지보수 절차 문서화**: `golden/README.md`에 갱신 커맨드(`-Dforge.golden.update=true`)·리뷰 체크리스트(배너/스크립트릿/이스케이프/MagicIAM API 회귀) 정리(이미 일부 존재 — 통합·보강).
- **AC**: 풀조합·엣지 골든 통과, 인젝션 회귀 확대, 전체 스위트 그린. 🔒 reviewer 결과확인(골든이 실제 안전 산출물).
- **의존**: P4-7 골든 골격 + P5/P5.5/P5-5c 골든셋. 저risk·독립 → P6-1 다음 착수 후보.

---

## 의존 순서 / 착수 계획

```
P6-1(런타임 버전관리, 저risk 독립) ──┐
P6-4(골든 심화, 저risk 독립) ────────┼─ 즉시 착수 가능(설계 포크 없음)
                                     │
P6-2(공통추출, 설계 포크) ───────────┤─ 스코프 확정 후 착수
P6-3(round-trip, 설계 포크) ─────────┘
```

- **P6-1·P6-4 먼저**(저위험·즉시가치·설계 포크 없음). **P6-2·P6-3은 스코프 결정 후**(추출 범위·왕복 방식).
- 보안 심장 무수정 원칙 유지, 계약 §11 부록 add-only(필요 축만).
