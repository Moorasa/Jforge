# J-FORGE 작업분해 — P7 스튜디오 UI/UX 전면 개편 (디자인 시스템 + 편의성 4종)

> **PM 산출물(2026-07-05).** 선행: P0~P6 전부 ✅(엔진 성숙 — 129+ tests, 골든/인젝션 회귀망, 계약 §0~§12).
> P7 성격: **엔진 무변경 페이즈.** 보안 심장(ScreenGenerator/AtomicFileWriter/PathSafetyService) 무수정,
> 골든 무손상(§0~§12 불변), 생성 템플릿(templates/gen) 및 RuntimeSyncer 복사 목록 **불가침**.
> 대상은 **빌더 자신의 도그푸딩 UI**(JSP+JS+CSS)와 빌더 도메인 API(화면 CRUD 확장·읽기전용 plan)뿐이다.
>
> **사용자 결정(2026-07-05)**:
> 1. 디자인 방향 = **빌더 전용 새 디자인** (JWorks 룩과 독립. 코드 규약 — 3종 세트·네임스페이스+IIFE·
>    스크립트릿 금지·textContent-only — 는 동일 유지)
> 2. 편의성 우선 4종 **전부 채택**: 슬롯 스캐폴드 캔버스 / 실물급 프리뷰 / undo·redo+단축키 /
>    화면 관리(이름변경·복제·삭제)+생성 dry-run
>
> 상태 표기: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료. 보안 태스크(🔒)는 reviewer 통과가 AC.

---

## 진단 요약 (2026-07-05 중간 점검)

| 구분 | 항목 | 근거 |
|---|---|---|
| **버릴 것** | 팔레트 내 슬롯픽커 UI | 배치 위치를 좌측 구석에서 묻는 어색한 흐름 → 캔버스 슬롯 직접 배치로 대체 |
| | propsPanel `autoSelectFirst` 임시 로직 | 주석에 "P3-5 후 교체 예정" 명시된 잔재. 선택 소유는 브리지로 일원화 |
| | `window.confirm/alert` 직접 호출 | 번들 jworks 위젯 미사용 = 도그푸딩 위반 (studioApp 2곳, palette 1곳) |
| | studioApp `setInterval` 폴링 | 새 화면 생성 후 select 반영 핵 → 목록 로드 완료 콜백으로 |
| | 4개 JSP 하드코딩 중복 셸 헤더 | home/project/catalog/studio 각각 복붙 → 공통 파셜로 |
| | home.jsp 디버그 스텁 | "runtime: jQuery…" 문구만 표시 → 대시보드로 |
| **개선할 것** | 화면 디자인 전면(회색 프로토타입 CSS) | 빌더 전용 디자인 시스템(P7-1) |
| | 캔버스: 빈 슬롯 비가시 | 아키타입 슬롯 스캐폴드 상시 표시(P7-2) |
| | 프리뷰: 근사 블록 렌더 | 번들 런타임 CSS + 샘플 데이터로 실물급 상향(P7-2) |
| | 팔레트: flat 목록 | 카테고리 그룹핑 + 검색 + 배치불가 dim(P7-2) |
| | 속성패널: 편집 대상 표시 없음 | 선택 인스턴스 헤더(모듈명·슬롯·삭제)(P7-2) |
| **추가할 것** | undo/redo + 키보드 단축키 | P7-3 |
| | 화면 이름변경·복제·삭제 UI | 서버 PUT/DELETE는 기존, 복제만 신규(P7-4) |
| | 생성 dry-run(파일 계획 미리보기) | 읽기전용 GenPlanner 신설(P7-4) |

---

## P7-1. 빌더 전용 디자인 시스템 + 공통 셸 — coder ✅ 완료

> **완료 메모(2026-07-05)**: `forge-theme.css` 신설(토큰 `--frg-*`: 다크 크롬 3단 + 라이트 캔버스 +
> 포인트/상태 + `--frg-field-*` 폼 토큰 — `.frg-dark` 스코프가 다크 값으로 재정의해 schemaForm 이
> 라이트(카탈로그)/다크(스튜디오 속성패널) 양쪽에서 동작). 공통 셸 파셜 `shell-top.jsp`(EL 조건식
> active, 스크립트릿 0) — 4개 페이지 중복 헤더 제거. home 대시보드(`home.js/css` 신설 — 히어로 +
> 프로젝트 카드 + 화면수 병렬 집계, textContent-only). project/catalog JSP·CSS 토큰화.
> `projectList.js` innerHTML 조립 → createElement/textContent 로 교정(규약 위반 해소) + 삭제
> jworks-confirm + 저장/삭제 스낵바. studioApp `window.confirm` 2곳 → `JWORKS_JSConfirm.start`
> (위젯 미로드 시에만 네이티브 폴백), 저장 성공 → 스낵바. 🔒 forge-theme.css 는 빌더 4페이지에만
> link — 프리뷰 iframe·RuntimeSyncer 목록·templates/gen 미포함(생성물 오염 0).

**디자인 컨셉 (확정)**: "다크 크롬 + 라이트 캔버스" 개발 도구 룩(Figma/IDE 계열).
셸(헤더·팔레트·속성패널)은 다크, 작업 결과물이 보이는 캔버스/콘텐츠 영역은 라이트 —
생성물(라이트 관리자 화면)이 캔버스에서 실물처럼 도드라진다.

- [ ] **`forge-theme.css`**: 디자인 토큰(CSS 변수 `--frg-*`) — 색(크롬/캔버스/포인트/상태), 타이포
      (NanumBarunGothic 유지 + stem·경로용 모노스페이스), 간격/라운드/그림자 스케일 + 공용 컴포넌트
      (버튼·인풋·셀렉트·뱃지·패널·모달) 클래스. **빌더 전용 파일** — 🔒 RuntimeSyncer 복사 목록·
      `templates/gen`·번들 매니페스트(생성물용)에 추가 금지(생성물 오염 방지).
- [ ] **공통 셸 파셜**: `views/admin/common/shell-top.jsp`(브랜드+주 메뉴+활성 탭 파라미터).
      4개 페이지의 중복 헤더 제거.
- [ ] **home 대시보드**: 프로젝트 카드(화면 수·타겟 경로) + 스튜디오 바로가기 + 최근 생성 이력 요약.
      디버그 문구 제거.
- [ ] **project/catalog/studio 3개 화면 테마 적용**: 기존 기능 무변경, 클래스/CSS만 교체.
- [ ] **jworks 위젯 대체**: `window.confirm` → `JWORKS_JSConfirm.start(title,text,cb(result))`,
      알림 → `JWORKS_JSSnackBar.create(text)` / `JWORKS_JSAlert.start`. 하드 confirm/alert 0.
- **AC**: 4개 화면 셸 일관 + 토큰 기반 스타일, `window.confirm|alert` grep 0(스튜디오 JS 기준),
  전체 테스트 그린(골든 무손상 — UI만 변경이므로 구조적으로 보장).

## P7-2. 슬롯 스캐폴드 캔버스 + 실물급 프리뷰 — coder ✅ 완료

> **완료 메모(2026-07-05)**: `slotMeta.js` 신설(슬롯 화이트리스트/라벨/순서 단일 소스 — 부모/iframe
> 공용, palette 의 로컬 상수 제거). `previewRenderer.js` 전면 재작성: 아키타입 슬롯 스캐폴드 상시
> 렌더(빈 슬롯 = 점선 존 + "+ 모듈 추가"), 모듈 마크업을 **골든 산출물 동형 구조**(section.search /
> table-view / card·tree·form / basic-info / associate-info)로 상향 + 샘플 데이터 — 프리뷰 iframe 이
> 번들 런타임 CSS 를 로드하므로 생성물과 같은 룩. 편집 오버레이(호버 헤더/선택 링/드래그)는 위에
> 겹치고 실물 컨테이너는 pointer-events:none. 배치 흐름: 팔레트 클릭 → 후보 1개 즉시 배치 / 복수 후보
> → 캔버스 슬롯 하이라이트(pending) 클릭 확정·Esc 취소, 슬롯 "+" → 배치 가능 모듈 선택 모달.
> 슬롯픽커 폐기. 팔레트: 카테고리 그룹핑 + 검색 + 배치불가 dim + 카테고리 색 뱃지(`[data-cat]`).
> 브리지 push 에 archetype/pending 동봉, MSG place/addRequest/cancelPending 추가(전 메시지 origin
> 검증 유지). 속성패널: 선택 헤더(모듈명·슬롯·instanceId·삭제) 추가. XSS 규약(textContent-only) 전
> 파일 유지, styleClass/type 은 클라 cssToken·화이트리스트 재검증.

- [ ] **브리지 확장**: 부모→iframe postMessage에 `archetype` 동봉(현 definition+selectedId).
      iframe이 슬롯 화이트리스트(아키타입별 슬롯 구조·허용 카테고리·cardinality)를 알게 한다.
      (상수는 palette.js `SLOT_WHITELIST`와 단일 소스로 공유 — 별도 공용 JS로 추출.)
- [ ] **슬롯 스캐폴드**: 캔버스가 아키타입의 **전체 슬롯 구조를 상시 렌더**. 빈 슬롯 = 점선 존 +
      슬롯 라벨 + "+ 모듈 추가" 버튼. 배치된 슬롯 = 모듈 카드(선택/삭제/드래그 순서이동 유지).
- [ ] **배치 흐름 개편(슬롯픽커 폐기)**:
      (a) 팔레트 클릭 → 후보 슬롯 1개면 즉시 배치, 여러 개면 **캔버스의 해당 슬롯들이 하이라이트**되어
      슬롯 클릭으로 확정(Esc/바깥 클릭 취소).
      (b) 캔버스 빈 슬롯 "+" → 그 슬롯에 배치 가능한 모듈만 추린 선택 팝오버 → 클릭 배치.
- [ ] **팔레트 개선**: 카테고리(FILTER/VIEW/ACTION/DETAIL…) 그룹핑 + 검색 인풋 + 현재 아키타입에
      배치 불가 모듈 dim 처리.
- [ ] **실물급 프리뷰**: 근사 블록 렌더를 **번들 런타임 CSS 클래스 마크업**(golden 산출물
      `src/test/resources/golden/*` 의 JSP 구조를 근거로 한 정적 재현) + **샘플 데이터**(예시 3행,
      카드 3장, 트리 2단 등)로 상향. 편집 오버레이(선택 테두리·삭제·그립)는 위에 겹치되
      마크업은 생성물과 동형.
- [ ] **속성패널 헤더**: 선택 인스턴스의 모듈명·슬롯 라벨·instanceId + 삭제 버튼.
- **AC**: 빈 화면에서도 슬롯 구조 가시, 슬롯픽커 코드 잔재 0, 프리뷰 마크업이 런타임 CSS 클래스
  기반(frg-prev-* 근사 스타일 잔존 최소화), XSS 규약(textContent-only) 유지.

## P7-3. undo/redo + 키보드 단축키 — coder ✅ 완료

> **완료 메모(2026-07-05)**: studioApp 에 히스토리 스택(JSON 직렬화 스냅샷, cap 50) —
> `updateDefinitionJson` 지점에서 직전 상태 push, 새 편집 시 redo 가지 소멸. 공개 API
> `undo/redo/canUndo/canRedo` + 상단바 ↶↷ 버튼. 화면/프로젝트 전환 = 리셋, 저장 = 유지.
> 단축키: Ctrl+S 저장(입력 중에도, 브라우저 다이얼로그 차단) / Ctrl+Z / Ctrl+Y·Ctrl+Shift+Z /
> Delete 선택 삭제 — 입력 요소 포커스 중엔 편집 단축키 무시. 잔재 정리: propsPanel
> `autoSelectFirst` 제거(선택은 브리지 단일 소유), studioApp `setInterval` 폴링 제거
> (`loadScreens` Promise 화 → 완료 콜백에서 select 반영).

- [ ] **히스토리 스택**: studioApp `updateDefinitionJson` 지점에 스냅샷(JSON 직렬화 깊은복사,
      cap 50) 축적. `studio.undo()/redo()/canUndo()/canRedo()` 공개 API + 상단바 버튼.
      화면 전환/저장 시 스택 리셋 정책: 화면 전환=리셋, 저장=유지(저장 후에도 undo 가능).
- [ ] **단축키**: Ctrl+Z(undo) / Ctrl+Y·Ctrl+Shift+Z(redo) / Ctrl+S(저장) / Delete(선택 삭제).
      입력 요소(input/textarea/select) 포커스 중엔 Delete/undo 단축키 무시(브라우저 기본 우선).
- [ ] **잔재 정리**: propsPanel `autoSelectFirst` 제거(선택은 브리지 단일 소유. 화면 로드 직후엔
      "모듈을 선택하세요" 상태가 정상), studioApp `setInterval` 폴링 제거(loadScreens Promise화).
- **AC**: 배치→속성수정→undo×n→redo×n 왕복 무손상(definition 바이트 동등), dirty 플래그 정확,
  단축키 동작, 임시로직 grep 0.

## P7-4. 화면 관리(이름변경/복제/삭제) + 생성 dry-run — coder (reviewer 🔒) ✅ 완료

> **완료 메모(2026-07-05)**: `screenMgmt.js` 신설 — 상단바 "⋯" 팝오버(이름변경 모달 → 기존
> `PUT /api/screens/{id}` / 복제 모달 → 신규 `POST /{id}/duplicate` / 삭제 → jworks-confirm →
> 기존 `DELETE`). studioApp 에 `reloadScreens(selectScreenId)` 공개 API(목록 갱신 + 재선택/해제).
> **복제 서버**: `ForgeScreenDuplicateRequest`(stem 정규식 동일) + `ForgeScreenService.duplicate` —
> slots 무가공 보존, stem 만 ObjectNode.put 교체(문자열 조립 0), 결과 전체 DefinitionValidator
> 재검증, DRAFT 시작. **dry-run**: 신규 `GenPlanner`(🔒 읽기전용, 파일쓰기/렌더 0) —
> TemplateContextBuilder 재사용(검증 동일), GenArtifacts 정적 맵 조회로 ScreenGenerator planArtifacts
> 미러, stub 경로는 `StubGenerator.planStubs`(신설 — generateStubs 와 물리적 단일 소스),
> 존재 판정은 `resolveSafeWritePath` 호출값의 `Files.exists`. 보안 심장 무수정.
> `GET /api/screens/{id}/generate/plan`. UI: 생성 버튼 → 계획 모달(신규 n · 덮어쓰기 m + 파일 목록
> + 루트 부재 경고) → 확정 시에만 실제 생성(계획 조회 실패 시 confirm 폴백). 테스트:
> `GenPlannerTest`(plan == 실산출(런타임 제외) 순서까지 동일 = 드리프트 회귀망 · 쓰기 0 ·
> 루트 부재 신호) + `ForgeScreenDuplicateTest`(slots 보존/stem 교체/DRAFT/재검증/404·400).
> **전체 134 tests / 0 failures / 2 skipped, BUILD SUCCESS** (기존 129 + 신규 5, 골든 무손상).

- [ ] **UI 결선(기존 API)**: 상단바 화면 셀렉트 옆 "관리" 메뉴 — 이름변경(`PUT /api/screens/{id}`),
      삭제(`DELETE /api/screens/{id}`, jworks-confirm 경유). 삭제/변경 후 목록 갱신.
- [ ] **복제(신규 서버)**: `POST /api/screens/{id}/duplicate` (body: 새 screenName/stem).
      DEFINITION_JSON의 slots는 보존, `stem/role/archetype` 골격 필드는 새 메타로 교체
      (ObjectNode 안전 조립 — 문자열 연결 0). stem 정규식·공통코드 검증은 기존 create 경로 재사용.
- [ ] 🔒 **생성 dry-run(읽기전용 plan)**: `GET /api/screens/{id}/generate/plan` →
      신규 `GenPlanner`(gen.pipeline 패키지, **쓰기 0**) — TemplateContextBuilder 재사용(검증 동일),
      GenArtifacts 정적 맵으로 아티팩트 목록 도출(ScreenGenerator.planArtifacts와 동일 규칙),
      StubGenerator stub 3종 경로 포함, 각 경로를 `resolveSafeWritePath`(호출만)로 해석 후
      `Files.exists`로 **신규/덮어쓰기** 판별. AtomicFileWriter 미사용(렌더도 안 함 — 경로 계획만).
- [ ] **UI**: 생성 버튼 클릭 → plan 조회 → "신규 n · 덮어쓰기 m" 파일 목록 모달 →
      jworks-confirm 확정 시에만 실제 `POST /generate`.
- [ ] **테스트**: duplicate(슬롯 보존+골격 교체+검증 거부), GenPlanner(plan 파일목록 =
      실제 generate 파일목록 동일 — 기존 골든 조합 화면으로 대조, 쓰기 0 확인).
- **AC**: 복제 화면 재오픈 정상(왕복 무손상), plan==실산출 목록 일치, 🔒 reviewer PASS
  (GenPlanner 쓰기 경로 부재·심장 무수정·duplicate 검증 경유), 전체 스위트 그린.

---

## P7-5. 사용성 개선 — 시작 가이드·기본 구성 프리셋·용어 순화 — coder ✅ 완료

> **사용자 피드백(2026-07-05)**: "사용 방법이 너무 어렵다, UI도 어렵다." 원인 진단: (1) 빈 캔버스가
> 다음 행동을 안내하지 않음(프로젝트를 다른 페이지에서 먼저 만들어야 하는 것을 알 수 없음),
> (2) stem/아키타입/role 등 계약 용어가 그대로 노출, (3) 새 화면이 빈 상태로 시작해 조립 진입장벽.
>
> **완료 메모(2026-07-05)**:
> - **시작 가이드 오버레이**(`studioGuide.js` 신설): 화면 미선택 동안 캔버스에 3단계
>   (① 프로젝트 연결 → ② 화면 만들기 → ③ 조립·파일 생성) 진행 표시 + 상태별 CTA.
>   프로젝트가 없으면 **스튜디오 안에서 바로 만드는 모달** 제공(studioApp 에 `reloadProjects`
>   공개 API 추가 — change 핸들러 재사용으로 신규 프로젝트 자동 선택). select 옵션 변화는
>   MutationObserver 로 관찰(허브 무수정 결합).
> - **새 화면 = 카드 + 프리셋**: 아키타입 코드 select → 한글 라디오 카드(관리 화면/단순 목록/
>   좌우 분할 + 한 줄 설명), role → "사용 영역" pill. **"기본 구성으로 시작"(기본 ON)** —
>   생성 직후 palette.`applyPreset` 이 유형별 기본 모듈을 순차 배치(MGMT: 검색+툴바+테이블+상세 /
>   SIMPLE: 검색+툴바+테이블 / DUAL: 좌우 프레임 — LAYOUT_FRAME 카테고리 VIEW 로 서버 화이트리스트
>   정합 확인). props 는 PROP_SCHEMA default, 일반 배치 경로(doPlace Promise 화) 재사용이라
>   undo 회수·서버 검증 동일.
> - **용어 순화**: stem → "파일 이름(영문)" + 생성 파일 예시 힌트, 생성 버튼 → **"파일 생성"** +
>   툴팁(저장=DB/생성=파일쓰기 구분), 홈에 "이렇게 사용하세요" 3단계 카드.
> - 프로덕션(Java) 무변경 — JS/JSP/CSS 만. 전체 스위트 영향 없음(134 green 유지).

---

## 의존 순서 / 착수 계획

```
P7-1(디자인 시스템·셸) ── 토큰이 이후 모든 UI의 기반 → 최우선
   └→ P7-2(캔버스·프리뷰) ── 스튜디오 핵심 체감
        └→ P7-3(undo/단축키) ── 편집 안정화(캔버스 개편 후 삽입점 확정)
             └→ P7-4(화면관리·dry-run) ── 서버 확장 포함, reviewer 🔒 게이트
```

- 전 태스크 공통 불변: 보안 심장 무수정 · `templates/gen` 불가침 · RuntimeSyncer 복사 목록 불변 ·
  golden 바이트 동일 · XSS 규약(textContent/createElement only, innerHTML 0) · 스크립트릿 0.
