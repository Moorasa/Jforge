<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jworks.forge.catalog.mapper.ModuleTypeMapper">

    <!-- 컬럼 명시(SELECT * 금지). 대문자스네이크 → 카멜 자동매핑.
         PROP_SCHEMA_JSON(JSONB)은 ::text 로 원문 문자열로 뽑는다(재해석/가공 없이 그대로 전달). -->
    <sql id="moduleTypeColumns">
        MODULE_TYPE_CODE, MODULE_NAME, CATEGORY_CODE,
        PROP_SCHEMA_JSON::text AS PROP_SCHEMA_JSON,
        TEMPLATE_KEY, PREVIEW_KEY, USE_YN, SORT_ORDER, REG_DTM, MOD_DTM
    </sql>

    <select id="selectActiveList" resultType="com.jworks.forge.catalog.domain.ModuleType">
        SELECT <include refid="moduleTypeColumns"/>
          FROM TB_FRG_MODULE_TYPE
         WHERE USE_YN = 'Y'
         ORDER BY SORT_ORDER, MODULE_TYPE_CODE
    </select>

    <select id="selectActiveByCategory" resultType="com.jworks.forge.catalog.domain.ModuleType">
        SELECT <include refid="moduleTypeColumns"/>
          FROM TB_FRG_MODULE_TYPE
         WHERE USE_YN = 'Y'
           AND CATEGORY_CODE = #{categoryCode}
         ORDER BY SORT_ORDER, MODULE_TYPE_CODE
    </select>

    <select id="selectActiveByCode" resultType="com.jworks.forge.catalog.domain.ModuleType">
        SELECT <include refid="moduleTypeColumns"/>
          FROM TB_FRG_MODULE_TYPE
         WHERE MODULE_TYPE_CODE = #{moduleTypeCode}
           AND USE_YN = 'Y'
    </select>

</mapper>
