<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jworks.forge.code.mapper.CommonCodeMapper">

    <select id="selectByGroup" resultType="com.jworks.forge.code.domain.CommonCode">
        SELECT GRP_CODE, CODE, CODE_NAME, SORT_ORDER, USE_YN
          FROM TB_FRG_COMMON_CODE
         WHERE GRP_CODE = #{grpCode}
           AND USE_YN = 'Y'
         ORDER BY SORT_ORDER, CODE
    </select>

</mapper>
