<#-- EMPTY_STATE — 캔버스 인라인 파셜(§17.4). -->
            <section class="dashboard-widget empty-state">
                <h2>${htmlText((props["title"])!"데이터가 없습니다")}</h2>
                <p>${htmlText((props["description"])!"")}</p>
                <button type="button">${htmlText((props["actionText"])!"새로 만들기")}</button>
            </section>
